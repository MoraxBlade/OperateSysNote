
# 11章 文件系统
## 11.1 块设备
### 11.1.1 块设备的基本概念
#### 定义
- 块设备：以固定大小数据块为单位进行读写的存储设备，与字符设备相对
#### 常见块设备类型
- 机械硬盘（HDD）：数据存储在磁道上，无法单字节读取
- 固态硬盘（SSD）：基于闪存技术，以块为单位读写
#### 块的大小
- 硬件物理块大小差异大；文件系统抽象为512字节或4KB
#### 日常现象解释
- 复制大量小文件更慢：每个文件需单独创建inode、更新目录项、分配数据块，元数据操作多；硬盘磁头反复寻道，机械延迟累积
### 11.1.2 Linux存储软件栈
| 层次                | 功能                               |
| ------------------- | ---------------------------------- |
| 设备驱动            | 与硬件通讯，控制读写操作           |
| I/O调度层           | 合并重排序I/O请求，优化磁头寻道    |
| 块层                | 提供统一块设备抽象，管理块设备     |
| 文件系统            | 管理存储上的文件系统格式和数据组织 |
| 虚拟文件系统（VFS） | 管理多个文件系统，提供统一抽象     |
| 页缓存              | 缓存文件数据，加速读写操作         |

## 11.2 基于inode的文件系统
### 11.2.1 文件的基本概念
#### 文件是有名字的字符序列（字节序列）
- 所有文件本质都是字符序列，不同类型仅字符组织方式不同
#### 文件的核心属性
- 文件名：人类可读标识符
- inode编号：操作系统内唯一标识
- 文件内容：存储在数据块中的字节序列
### 11.2.2 inode结构
#### 概念
- inode是文件系统管理文件的核心数据结构，每个文件对应唯一inode
- 分级索引结构，类似多级页表，降低组织管理代价
#### inode的组成部分
##### 元数据区域
- 文件类型、大小、权限、拥有者、时间戳
##### 指针区域
- 直接指针：直接指向数据块，用于小文件
- 间接指针：指向索引块，索引块再指向数据块
- 二级间接指针：指向二级索引块，用于大文件
- 关键结论：inode元数据中不包含文件名，文件名存储在目录中
#### inode的大小示例
- 不同文件系统inode大小可配置，常见128/256/512字节
#### 计算inode最大可表示的文件大小
- 假设：块大小=4KB，指针大小=8字节
- 每个索引块可存放指针数 = 4KB / 8B = 512个

| 指针类型        | 计算方式            | 可表示数据量 |
| --------------- | ------------------- | ------------ |
| 12个直接指针    | 12 × 4KB            | 48KB         |
| 3个间接指针     | 3 × 512 × 4KB       | 6MB          |
| 1个二级间接指针 | 1 × 512 × 512 × 4KB | 1GB          |
- 总计：48KB + 6MB + 1GB ≈ 1.006GB
### 11.2.3 目录文件与文件名
#### 概念
- 目录本质是文件，内容为“文件名 → inode编号”的映射表
#### 目录项结构
- 目录的indo
- 目录项
  - indoe
  - 目录项长度
  - 文件名长度
  - 文件名
#### 目录操作
- 查找文件：遍历目录项匹配文件名，获取inode编号
- 删除文件：将目录项inode号置0，标记为无效
- 合并优化：连续无效目录项可合并释放空间
#### 关键结论：文件名存储在目录文件中，而非inode中
- 一个文件可对应多个文件名（硬链接）
- inode编号相同即为同一文件
#### 思考题
- 目录大小取决于目录项数量，而非文件内容大小；文件数量越多，目录占用空间越大
### 11.2.4 文件系统的存储布局
#### 概念
- 文件系统格式化时创建统一存储布局，分配inode节点与数据块空间
#### 布局组成
##### 超级块(super block)
- 存储文件系统基本元信息
##### 魔法数字(magic number)
- 识别文件系统类型
##### inode节点数量
- 决定文件系统最多可存储的文件数量
##### inode分配信息
- 位图表示，1标记对应inode已使用
##### 块分配信息
- 位图表示，1标记对应数据块已使用
#### 空间计算示例（/dev/sdb2分区）
| 项目        | 计算              | 大小   |
| ----------- | ----------------- | ------ |
| 数据区容量  | 234,159,616 × 4KB | 937GB  |
| inode表空间 | 58,540,032 × 256B | 15GB   |
| inode位图   | 58,540,032 / 8    | 7.3MB  |
| 数据块位图  | 234,159,616 / 8   | 29.3MB |
- 说明：位与字节换算需除以8
#### 日常现象解释：为什么1TB硬盘实际可用只有约931GB？
- 厂商按十进制：1TB = 10¹²字节
- 操作系统按二进制：1GB = 2³⁰字节
- 换算：1TB = 10¹² / 2³⁰ ≈ 931.3GB
- 叠加文件系统元数据开销，实际可用空间更少

## 11.3 基于表的文件系统
### 11.3.1 FAT（文件分配表）
#### 核心数据结构
- FAT表：数组结构，每个表项对应一个簇，存储下一个簇的编号；文件通过链表组织
- 引导记录区：功能类似超级块
#### 文件读取流程
1. 从目录项获取文件起始簇号
2. 读取该簇数据
3. 查询FAT表获取下一个簇号
4. 重复直到遇到结束标记FFFF
#### FAT的优缺点
- 优点：实现简单，兼容性好，广泛应用于可移动存储
- 缺点：大文件随机访问慢；链表易损坏，需双FAT备份；FAT32最大文件仅4GB；无权限、日志等高级特性
#### exFAT：U盘常用格式
- 与FAT32不兼容；使用位图加速空间分配；支持Unicode长文件名；目录查找用哈希对比；支持4GB以上文件；元数据校验码保证完整性
### 11.3.2 NTFS（新技术文件系统）
#### 核心特性
- 位图管理空间分配；Unicode长文件名；哈希查找提升目录速度；支持大文件（8字节表示文件大小）；元数据校验码
#### NTFS存储布局
- MFT（主文件表）：类似inode表，存储文件和目录记录
- MFT镜像：备份，提升容错性
- 日志文件：支持事务和恢复
- B+树索引：提升目录查找效率
##### MFT中的文件记录
- 小文件：数据直接存储在MFT记录中（常驻属性）
- 大文件：数据存数据区，MFT记录存储指向数据区段的指针
#### B+树的优势
- 查找、插入、删除时间复杂度稳定为O(log n)
- 磁盘I/O次数少（树高低）
- 适合范围查找和顺序遍历

## 11.4 使用文件系统
### 11.4.1 硬链接与符号链接
#### 硬链接（Hard Link）
##### 原理
- 多个目录项指向同一个inode编号，共享同一份文件数据
##### 特点
- 多个文件名地位完全相同
- 删除其中一个不影响其他，仅inode引用计数降为0时才真正删除数据
- 不能跨文件系统；不能链接目录（防止循环引用）
#### 符号链接（Symbolic Link）
##### 原理
- 特殊文件，内容存储目标文件的路径
##### 特点
- 独立于目标文件存在；目标路径改变后链接失效（悬空链接）
- 可跨文件系统；可链接目录
##### 作用
- 提供多元文件读取方式
##### 常见用途
- 动态库版本管理
### 11.4.2 文件操作的基本接口
#### 复制文件
- 核心流程：参数校验→打开源文件→创建目标文件→循环读写拷贝→关闭文件描述符
- 关键函数：open、creat、read、write、close
#### 打开文件
- 接口：open、openat
- 常用标志：O_RDONLY、O_WRONLY、O_RDWR、O_CREAT、O_TRUNC
#### 读写文件
- 接口：read、write
#### 移动读写位置
- 接口：lseek
- 基准位置：SEEK_SET、SEEK_CUR、SEEK_END
#### 获取文件属性
- 接口：stat、fstat、lstat
- 区别：stat跟随符号链接；lstat不跟随，获取链接本身信息
#### 关闭文件
- 接口：close
#### fork与文件描述符的共享
- fork后子进程复制父进程文件描述符表，父子共享同一文件偏移量
- 结果：父子进程连续读取文件内容不重叠，取决于调度顺序
### 11.4.3 页缓存与脏页
#### 页缓存机制
- 操作系统设置文件缓存，避免每次read/write都访问硬盘；数据先写入内存缓存
#### 脏页回写：防止死机、断电等
- 脏页：内存中已修改但未写回磁盘的缓存页
- 回写触发：内核定期flush线程刷回；内存压力大时触发回收
#### 强制同步
- 接口：fsync，强制将指定文件所有脏页写入磁盘，保证数据持久化
### 11.4.4 内存映射（mmap）
#### 核心思想
- 将文件内容直接映射到进程虚拟地址空间，文件读写退化为内存操作
#### mmap接口
- 接口：mmap、msync、munmap
- 权限参数：PROT_READ、PROT_WRITE、PROT_EXEC
- 映射标志：MAP_SHARED（共享，修改写回文件）、MAP_PRIVATE（私有，修改不写回）
#### 使用实例
- 打开文件→映射到内存→直接操作内存修改文件内容
#### mmap的实现原理
1. 调用mmap时分配虚拟地址空间，记录地址与文件inode映射关系
2. 访问映射地址触发缺页中断
3. 缺页中断处理函数通过虚拟地址找到inode，从磁盘读入物理内存
4. 建立虚拟地址与物理页映射，进程继续执行
#### mmap的优势
- 随机访问高效，无需频繁lseek
- 减少系统调用次数，一次映射后续均为内存操作
- 减少数据拷贝，无需用户态中间buffer
- 访问局部性好，利用预读提升性能
- 可配合madvise提供访问模式提示
### 11.4.5 文件系统的高级功能
#### 基于mmap的文件复制实现
- 流程：打开源文件与目标文件→获取源文件大小→扩展目标文件→分别映射到内存→内存拷贝完成复制→解除映射关闭文件
#### 文件克隆（写时复制）（快照）
- 原理：仅复制inode元数据，原始数据块共享；修改时触发复制，仅修改部分分配新块
- 文件系统快照基于相同原理
##### 实现方式
- 基于inode的文件系统：复制inode表作为快照，标记数据区为写时拷贝
- 树状结构文件系统：复制树根作为快照，标记下层节点为写时拷贝
#### 其他高级功能
| 功能     | 说明                           |
| -------- | ------------------------------ |
| 加密     | 数据落盘自动加密               |
| 压缩     | 透明压缩存储，节省空间         |
| 去重     | 重复数据块只存一份             |
| 校验     | 数据和元数据校验和，保证完整性 |
| 配额管理 | 限制用户/组可用空间            |
| 软件RAID | 多设备管理，提供冗余或条带     |
| 子卷     | 独立的文件系统命名空间         |
| 事务     | 原子操作，保证一致性           |

## 11.6 用户态文件系统（FUSE）
### 11.6.1 FUSE的设计动机
- 内核开发文件系统难度大、调试困难、出错易导致系统崩溃、迭代周期长
- FUSE允许用户态实现文件系统，降低开发门槛
#### 优势：
- 快速试验新设计；可用用户态第三方库；方便调试；无需担心内核崩溃；快速实现新功能
### 11.6.2 FUSE的工作流程
1. FUSE文件系统向FUSE驱动注册挂载
2. 应用程序发起文件请求
3. VFS根据挂载点转发请求给FUSE驱动
4. FUSE驱动将请求发送给用户态FUSE文件系统
5. 用户态文件系统处理请求并返回结果
6. FUSE驱动通过VFS返回结果给应用
#### FUSE的瓶颈
- 用户态与内核态上下文切换开销；消息传递延迟；不适合高性能场景
### 11.6.3 FUSE的应用
- SSHFS、NTFS-3G、GMailFS、WikipediaFS、网盘同步、分布式文件系统
### 11.6.4 FUSE示例：Hello World文件系统
- 核心：实现read等文件操作，通过fuse_main注册运行

## 11.7 大模型生成文件系统
### 11.7.1 背景与挑战
- 文件系统开发依赖复杂；大模型存在幻觉生成不稳定；简单语言易产生语义歧义
### 11.7.2 规约驱动的文件系统生成
- 使用规约描述文件系统行为，由大模型生成代码实现
#### 功能规约示例（霍尔三元组）
- {前置条件} 函数调用 {后置条件}
#### 并发规约
- 描述并发场景行为要求，保证线程安全
#### 模块化规约
- Rely：模块依赖的外部接口
- Guarantee：模块保证的行为
### 11.7.3 实验结果
- 主流大模型功能性测试与并发测试通过率较高，小模型通过率较低

## 11.8 思考题
### FAT文件系统为什么不支持硬链接？如何修改？
#### 困难：
- FAT用簇链表组织文件，无统一inode唯一标识
- 目录项直接存储起始簇号，多目录项指向同一簇链会破坏链表结构
- 删除一个链接时难以判断是否应释放数据
#### 修改方案：
- 引入引用计数，在FAT表中增加计数字段
- 目录项存储inode编号而非簇号，建立统一inode表
- 删除时减少引用计数，计数为0时才释放簇链
- 本质是向inode式文件系统靠拢