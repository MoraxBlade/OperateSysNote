
---

### 第1周：操作系统概述
*   **历史里程碑实例**：
    *   GM-NAA I/O (1956, IBM 704)：首个实用批处理OS。
    *   OS/360 (1964)：首次软硬件分离（ISA定义）；Fred Brooks《人月神话》。
    *   UNIX (1969)：C语言重写（1973），引入shell/管道。
    *   Linux (1991)：基于MINIX启发。
    *   Xerox Alto (1973)：首个图形化OS（鼠标/桌面）。
    *   iOS (2007)：基于Mach+BSD，Cocoa Touch。
    *   Android (2008)：基于Linux内核，OHA联盟。

---

### 第1周：操作系统结构
*   **失败架构实例**：
    *   **瓦萨号战舰**：结构缺陷导致首航倾覆。
    *   **N1火箭**：过于复杂（对比土星五号），登月失败。
    *   **IBM Workplace OS**：操作系统“瓦萨号”失败案例。
*   **简单内核**：MS-DOS
*   **代码规模实例**：UNIX V6 (1万行) → Linux 5.7 (2870万行) → Windows 10 (超1亿行)。
*   **宏内核结构实例**：**Linux**（超2000万行代码，结构性缺陷：无法通过ASIL-D汽车安全认证）。
*   **微内核实例**：
    *   **Mach** (CMU)：性能比UNIX差25%~67%，用于NeXT，后演变为macOS内核。
    *   **L3/L4**：IPC比Mach快20倍（使用寄存器传参）。
    *   **seL4**：首个形式化验证内核（8700行C代码，bug-free）。
    *   **QNX** (Neutrino)：用于波音等航天/汽车领域。
    *   **MINIX**：内置于Intel ME模块（管理引擎）。
    *   **Google Fuchsia**：Zircon微内核。
    *   **鸿蒙 (HarmonyOS)**：分布式软总线。
*   **混合内核实例**：**Windows NT**（微内核 + 内核态Executive）；**macOS/iOS**（Mach + BSD 4.3）。
*   **外核实例**：**Exokernel**（时间切片，单切片内只服务一个应用）。
*   **多内核实例**：**Barrelfish** (ETH Zurich)；**Popsicle Linux**（异构ARM核，各核独立内核实例）。
*   **Android框架实践**：
    *   **HAL层**：规避GPL协议，厂商无需开源驱动。
    *   **ART vs Dalvik**：ART采用AOT预编译（安装时转机器码），Dalvik采用JIT运行时编译。
    *   **Binder IPC**：服务组件化，可扩展性、可维护性、单服务崩溃不影响整体内核
---

### 第2周：ARM硬件环境与指令集
*   **芯片实例**：
    *   移动端：华为麒麟9000 (5nm)、高通骁龙8、苹果A17、联发科天玑。
    *   PC/服务器：苹果M1/M4、华为鲲鹏920。
*   **ARM架构版本**：ARMv8（引入64位AArch64，硬件加速加密）；ARMv9（侧重AI/安全）。
*   **硬件运行机制实例**：
    *   **寄存器多模态**：`X0`（64位）与 `W0`（32位，操作高位清零）。
    *   **SIMD寄存器**：`Vn`（128位），32位形式 `Sn`，64位形式 `Dn`。
*   **汇编指令实例**：
    *   移位：`asr`（算术右移，符号位填充）、`lsl`（逻辑左移，补0）、`ror`（循环右移）。
    *   访存：`LDR`（Load）和 `STR`（Store）。
    *   寻址方式：基址 `[Xn]`；前变址 `[Xn, #offset]!`；后变址 `[Xn], #offset`。
    *   分支：`cbz`（寄存器为0跳转）；`bne`（不等跳转，依赖PSTATE条件码N/Z/C/V）。
*   **函数调用实例（PCS准则）**：
    *   参数传递：`X0~X7`；返回值：`X0`。
    *   保存规则：调用者保存 `X9~X15`；被调用者保存 `X19~X28`。
    *   返回地址：`X30 (LR)`；帧指针：`X29 (FP)`；栈指针：`SP (X31)`。

---

### 第3/4周：虚拟内存管理
*   **进程内存布局查看实例**：
    *   命令：`cat /proc/[pid]/maps`（查看代码段、数据段、堆、栈、内核空间）。
    *   命令：`readelf -h` 和 `readelf -S` 查看ELF头部及Section信息（.text, .data, .bss, .rodata）。
*   **页表管理实践代码**：
    *   遍历页表：`get_next_pgtbl_page` 处理页表空洞。
    *   添加映射：`add_mapping`（逐级L0-L3，填充物理地址）。
    *   删除映射：`delete_mapping`（清空并刷新TLB：`flush_tlb`）。
*   **映射策略实例**：
    *   **立即映射**：`mmap` 调用时马上 `alloc_page` + `add_mapping`。
    *   **延迟映射**：仅记录 `vm_area_struct`，首次访问触发缺页异常（`do_page_fault`）时才分配物理页。
*   **扩展功能实例**：
    *   **写时拷贝 (COW)**：`fork()` 时共享页表并设只读，缺页异常时拷贝（`do_wp_page`）。
    *   **内存去重 (KSM)**：内核扫描合并相同内容物理页（潜在安全漏洞：侧信道）。
    *   **内存压缩 (zswap)**：换出时压缩存入内存（减少磁盘I/O）。
*   **计算题（大页大小推导）**：
    *   **题目**：若采用3级页表映射（36位页号均分为3等份），L1页表项指向的大页空间为多少？
    *   **解析**：每级索引位数 = 36/3 = 12位。L2页表含 2¹² = 4096 项，每项指向4KB物理页。
    *   **答案**：L1项覆盖范围 = 4096 × 4KB = **16 MB**。
    *   （注：ARMv8 4级页表中，L2或L1项可通过标志位指向2MB或1GB大页）。

---

### 第5周：物理内存管理
*   **碎片实例**：分段机制产生**外部碎片**；分页机制产生**内部碎片**。
*   **伙伴系统 (Buddy System) 实例**：
    *   管理结构：链表数组（按2的幂次分组）。
    *   分配逻辑（如申请15KB）：分配32KB块，分裂为两个16KB，分配一个。
    *   合并条件：互为伙伴（物理地址仅一位不同）。
*   **SLAB分配器实例**：
    *   **起源**：Solaris 2.4 (Jeff Bonwick)；Linux默认 **SLUB**（从2.6.23起）。
    *   结构：每个CPU有 `current`、`partial`、`full` slab链表。
*   **Linux内存管理实践**：
    *   用户态：`malloc/free`（glibc ptmalloc，底层用 `brk`/`mmap`）。
    *   内核态：`page` 结构体（巨大数量，使用联合体节省内存）。
    *   扩展：**CMA**（预留连续内存给驱动）、**OOM Killer**（内存耗尽杀进程）。

---

### 第6周：进程与线程
*   **进程状态转换实例**：`fork()` 后进入就绪态；调度后进入运行态；`fgets` 等待输入进入阻塞态；`return` 后进入终止态。
*   **数据结构实例**：Linux `task_struct`（包含PID、`mm_struct`、`files_struct`、`thread_struct`）。
*   **上下文切换实操**：
    1. 保存当前进程通用寄存器/PC/SP至PCB。
    2. 切换页表基地址（`ttbr0_el1`）。
    3. 切换内核栈（`sp_el1`）。
    4. 恢复下一进程上下文，`eret` 返回用户态。
*   **进程创建API实例**：
    *   `fork()`：完整拷贝（写时拷贝优化）。
    *   `vfork()`：共享地址空间（阻塞父进程，仅用于 exec）。
    *   `clone()`：可选择 `CLONE_VM` 等标志。
    *   `posix_spawn()`：fork+exec组合（更高性能）。
*   **文件描述符共享实例**：`fork()` 后父子进程 `read` 同一文件，文件偏移共享（读出数据不重复，互斥）。
*   **线程API实例 (`pthread`)**：
    *   代码演示：`pthread_create` 启动3个线程累加 `balance`。
    *   **计算题**：单线程运行（`pthread_join` 串行），最终结果确定；若并发无锁，最终结果不确定（竞争条件）。
*   **纤程/协程实例**：
    *   C语言：`ucontext_t`（`makecontext`/`swapcontext`）。
    *   Go语言：`go func()` 启动协程；`channel` 通信。
    *   **Go协程实践**：`select` 多路复用；质数筛选算法（筛法）实现。

---

### 第7周：调度
*   **经典调度策略实例**：
    *   **FCFS**（先到先得）：简单，平均周转时间长。
    *   **SJF**（最短任务优先）：平均周转短，但可能导致饥饿。
    *   **RR**（时间片轮转）：响应快，但牺牲周转时间（时间片过短导致上下文切换开销大）。
*   **优先级调度实例**：多级反馈队列（MLFQ）——初始高优先级，用完时间片降级，定时提升避免饥饿。
*   **优先级反转实例**：低优先级任务占据共享资源导致高优先级任务阻塞；解决方案：**优先级继承**。
*   **公平共享调度实例**：
    *   **彩票调度**：随机数分配tickets；支持**彩票转让**（客户端转给服务端）。
    *   **步幅调度**：确定性版本。计算：`stride = MAX / tickets`，每次调度pass最小的，累加stride。
*   **实时调度实例**：
    *   **RM (Rate-Monotonic)**：周期越短优先级越高（静态）。
    *   **EDF (Earliest Deadline First)**：动态截止时间优先（最优调度，条件：总利用率 U≤1）。
*   **多核调度实例**：
    *   协同调度（Gang Scheduling）：将无依赖任务分组。
    *   **PELT (Per-Entity Load Tracking)**：Linux 3.8引入，衰减因子 `γ³² = 0.5`（32周期后负载减半）。
*   **硬件实例**：
    *   **Intel 12代酷睿 / Apple A15**：大小核（性能核+能效核）。
    *   能耗公式：`W = ½ V² f`（调节频率调功耗）。
*   **实操命令**：`sched_setaffinity` 绑定CPU核心；`sysbench` 测调度时延（实测最大104ms，确定性差）。

---

### 第8周：进程间通信 (IPC)
*   **共享内存实例**：通过修改页表映射实现；问题在于**轮询**导致CPU资源浪费。
*   **消息传递实例**：
    *   **直接通信**：`Send(P, msg)` / `Recv(Q, msg)`（阻塞/非阻塞）。
    *   **间接通信**：通过**信箱**（如聊天群）。
*   **管道实例**：Unix命令 `ls | grep`；Xv6管道数据结构（环形缓冲区）；特点：单向、字节流、固定大小。
*   **消息队列实例**：带**类型**的链表；支持按类型查询（如 `Recv(type)`）。
*   **实践库**：**Protobuf**（跨语言序列化，字段号不可变更）。

---

### 第8/9/10周：同步原语
*   **硬件原子操作实例**：
    *   **x86**：`LOCK` 总线锁实现 `CAS`。
    *   **ARM**：`LL/SC`（Load-Linked / Store-Conditional）实现 `CAS`。
*   **互斥锁实现**：
    *   **自旋锁**：`while(CAS(lock,0,1)!=0)`。
    *   **排号锁 (Ticket Lock)**：`atomic_FAA(&next,1)` 拿号，轮询 `owner != my_ticket`（保证公平）。
*   **条件变量实例**：
    *   `pthread_cond_wait`（原子放锁+挂起），`pthread_cond_signal`（唤醒）。
*   **信号量 (PV原语)**：
    *   `wait(S)`：`S--`，若 `<0` 阻塞。
    *   `signal(S)`：`S++`，若 `≤0` 唤醒。
*   **读写锁实例**：
    *   **偏向读者**：读者计数器加锁，第一个读者拿写锁，最后一个释放。
    *   **偏向写者**：有写者等待时，新读者阻塞。
*   **RCU锁实例**：写者复制数据（`rcu_assign_pointer`），宽限期后释放（`synchronize_rcu`）；读者零开销（只禁用抢占）。
*   **死锁实例**：哲学家就餐问题；经典两锁交叉申请（`proc_A lock(A), lock(B)` vs `proc_B lock(B), lock(A)`）。
*   **银行家算法计算题（实战）**：
    *   **场景**：两个线程（A、B），两类资源（A类、B类各1份）。
    *   **分配表**：
        | 线程 | Max (A,B) | Allocation (A,B) | Need (A,B) | Available |
        | ---- | --------- | ---------------- | ---------- | --------- |
        | A    | (1,1)     | (1,0)            | (0,1)      | (0,1)     |
        | B    | (1,1)     | (0,0)            | (1,1)      | -         |
    *   **过程**：
        1. 模拟可用资源 (0,1)。
        2. 检查线程A：Need (0,1) ≤ (0,1) → 可执行。执行完释放 (1,0) → 可用变为 (1,1)。
        3. 检查线程B：Need (1,1) ≤ (1,1) → 可执行。
    *   **结论**：安全序列为 **A → B**（避免死锁）。

---

### 第11周：多核与多处理器
*   **真实硬件性能数据**：
    *   **鲲鹏服务器**：64核心，4个NUMA节点（0-3），节点距离（10, 15, 20）。
    *   **Intel Xeon vs AMD Opteron**：远程内存访问时延（Load/Store）随跳数增加（1-hop约117 cycles，2-hop约498 cycles）。
*   **缓存一致性实例（MSI协议）**：
    *   状态迁移：独占(Modified) → 其他核读(Shared) → 其他核写(Invalid)。
*   **可扩展性瓶颈实践**：
    *   单一缓存行竞争导致性能断崖（性能不升反降）。
    *   **优化方案**：回退锁（指数退避）；**MCS锁**（每个CPU自旋于本地标志，避免全局缓存行争用）。
*   **虚假共享 (False Sharing) 实例**：
    *   代码：`struct { int a; int b; }` 在同一缓存行（64B），多核分别修改a和b导致大量一致性同步。
    *   **解决方案**：填充 `char pad[64]` 隔离。
*   **内存模型实例**：
    *   **TSO** (x86/64)：允许写-读重排，使用**Store Buffer**。
    *   **Weak-Ordering** (ARM)：允许更多重排，需 `barrier` 指令。

---

### 第12/13周：文件系统
*   **实操命令**：
    *   `stat` 查看文件大小与占用空间（Blocks字段）。
    *   `ls -i` 查看inode编号。
    *   `dumpe2fs -h /dev/sda` 查看超级块信息（Inode count, Block count, Inode size一般为256B）。
*   **inode结构实例（计算题）**：
    *   **假设**：12个直接指针 + 3个间接指针 + 1个二级间接指针（块大小4K，指针8B）。
    *   **计算**：
        *   直接寻址：12 × 4KB = 48KB。
        *   单间接：每个索引块含 `4KB / 8B = 512` 个指针 → 512 × 4KB = 2MB。
        *   3个单间接：6MB。
        *   二级间接：512 × 2MB = 1GB。
        *   **最大文件大小 ≈ 48KB + 6MB + 1GB ≈ 1.006 GB**。
*   **目录存储实例**：目录项存（文件名 + inode编号），硬链接（`ln`）本质是多个目录项指向同一inode；符号链接（`ln -s`）存路径。
*   **文件复制实践**：
    *   传统 `cp`：`read` + `write`（两次数据拷贝）。
    *   **mmap 优化**：`mmap` + `memcpy`（仅一次拷贝到用户态）。
    *   **文件克隆**：仅拷贝inode表元数据（快照/写时拷贝）。
*   **文件系统崩溃一致性实例**：
    *   工具：Windows `chkdsk`；Linux `fsck`。
    *   **JBD2日志模式**：
        1. **写回 (Writeback)**：只记录元数据，最快，一致性最差。
        2. **顺序 (Ordered)**：数据先于元数据落盘（Linux默认）。
        3. **日志 (Journal)**：元数据和数据全记录（最安全，写两次）。
*   **日志文件系统 (LFS)**：顺序写入日志；空间回收通过**段清理**（读入有效数据，写回干净段）。

---

### 第14/15周：设备管理
*   **设备交互实例**：
    *   **PIO (MMIO)**：树莓派串口初始化代码（`early_put32` 配置GPFSEL寄存器）。
    *   **DMA**：流程：CPU初始化DMA控制器（方向/地址/大小）→ DMA传输 → DMA中断通知CPU。
*   **中断实例**：
    *   查看命令：`cat /proc/interrupts`（显示硬中断，如网卡eth0）；`cat /proc/softirqs`（显示软中断 `NET_RX`/`NET_TX`）。
    *   上半部（硬中断）：`request_irq`；下半部（软中断）：`Tasklet` / `Workqueue`。
*   **驱动模型实例**：
    *   **LDDM**（Linux设备驱动模型）：`bus_type`（PCI/USB），`device`，`device_driver`。
    *   **设备树 (DTS)**：`st,stm32-uart` 节点（描述compatible, reg, interrupts）。
*   **实操命令**：
    *   `lsblk` 查看块设备。
    *   `lspci` 查看PCI设备（如Matrox G200eW3显卡）。

---

### 第15/16周：系统虚拟化
*   **Type-1 VMM实例**：Xen, VMware ESXi（直接运行于硬件）。
*   **Type-2 VMM实例**：QEMU/KVM, VirtualBox（依赖宿主机OS）。
*   **CPU虚拟化实例**：
    *   **Intel VT-x**：Root模式（VMM）与非Root模式（VM）；**VMCS** 结构（保存Guest/Host状态）。
    *   **ARM**：EL2特权级；VHE（ARMv8.1）允许Host OS运行于EL2。
*   **内存虚拟化实例**：
    *   **影子页表**（软件模拟GVA→HPA）。
    *   **Stage-2页表**（硬件EPT/NPT）：GVA→GPA→HPA，两阶段缺页异常分开处理。
*   **I/O虚拟化实例**：
    *   **设备模拟 (QEMU)**：捕捉MMIO/PIO指令（如e1000网卡）。
    *   **半虚拟化 (VirtIO)**：前后端驱动，通过共享环（`Descriptor Table` + `Available Ring` + `Used Ring`）批量传递I/O请求。
*   **QEMU/KVM实践**：
    *   QEMU运行于用户态（策略/设备模拟），KVM运行于内核态（机制/硬件虚拟化）。
    *   QEMU通过 `/dev/kvm` 的 `ioctl`（`KVM_CREATE_VCPU`, `KVM_RUN`）进行交互。
    *   流程：`ioctl(KVM_RUN)` → KVM加载VMCS → `VMLAUNCH`/`ERET` 进入VM → VM Exit → KVM处理 → QEMU模拟I/O。

---

### 第17周：操作系统安全
*   **访问控制实例**：
    *   **DAC**：文件权限（`rwx`），`/etc/shadow` 用户不可读。
    *   **SUID**：`/usr/bin/passwd` 具有SUID位（`-rwsr-xr-x`），运行时提权为root。
    *   **RBAC**：用户与角色解耦（如管理员/普通用户）。
    *   **MAC (BLP模型)**：简单安全属性（不可读更高密级），星属性（不可写更低密级）。
*   **SELinux实例**：`ls -Z` 查看安全上下文（`user:role:type:mls`）；AVC缓存加速权限检查。
*   **漏洞实例**：
    *   **整数溢出**：`count = 2^30`，`sizeof=8`，`vmalloc(2^30 * 8)` 导致越界。
    *   **ret2usr / ret2dir**：内核执行用户态代码（绕过SMEP利用直接映射区）。
    *   **KASLR**：内核地址随机化防御。
*   **侧信道攻击实例**：
    *   **时间信道**：`checkpw` 逐位比较密码，利用返回时间差异逐位爆破（62^8 → 62×8次）。
    *   **缓存信道 (Flush+Reload)**：攻击者清空缓存，等待受害进程执行，重载变量测时延判断是否被访问。
    *   **Meltdown/Spectre**：利用CPU预测执行跨权限读取内核数据，通过缓存侧信道泄露（最高可达503KB/s）。
    *   **防御**：KAISER（页表隔离），ORAM（不经意随机访问内存）。
*   **可信执行环境 (TEE) 实例**：
    *   **Intel SGX**：硬件加密内存（PRM），Merkle Tree保护完整性，远程验证（Remote Attestation）。
    *   **AMD SEV**：虚拟机粒度加密（每个VM独立密钥）。

---

### 第17周：网络协议栈与系统实现
*   **网络编程实例**：
    *   I/O多路复用：`select` / `poll` / `epoll`（解决C10K问题）。
*   **收包流程数据拷贝（计算题）**：
    *   **题目**：从网卡到用户进程，数据至少拷贝几次？
    *   **解析**：
        1. 网卡DMA拷贝到内核RX Ring（**第1次**）。
        2. `sk_buff` 不拷贝数据（仅指针调整），省略一次。
        3. 协议栈处理不拷贝数据（指针移动），省略一次。
        4. 用户态调用 `read`/`recvfrom`，内核将数据从 `skb` 拷贝到用户缓冲区（**第2次**）。
    *   **答案**：**至少2次**。
*   **`sk_buff` 操作实例**：
    *   `alloc_skb` → `skb_reserve` (预留头部) → `skb_put` (添加负载) → `skb_push` (压入头部)。
    *   `skb_clone` (浅拷贝，共享数据) vs `skb_copy` (深拷贝)。
*   **性能优化实例 (DPDK)**：
    *   **核心思想**：控制面与数据面分离（绕过Linux内核协议栈）。
    *   **PMD (Poll Mode Driver)**：用户态轮询取代中断，通过 `/dev/uioX` 和 `mmap` 零拷贝访问网卡。
    *   **大页 (Huge Page)**：2MB/1GB页减少TLB Miss。
    *   **无锁环 (Ring Buffer)**：多生产者/消费者入队出队依赖 `CAS` 原子操作移动 `prod_head`/`cons_tail`（避免锁开销）。
    *   **内存池 (MemPool)**：内存对齐避免跨通道访问延迟；私有缓存（Per-Core Cache）减少竞争。
*   **硬件加速实例**：
    *   **RDMA**：绕过内核，直接远程内存访问（零拷贝）。