# 第二章 操作系统结构

## 2.1 操作系统的机制与策略
### 设计原则：策略与机制分离
  - 策略（Policy）：做什么，相对动态
  - 机制（Mechanism）：怎么做，相对静态
### 好处
  - 调整策略适应不同应用需求
  - 优化机制完善对策略的支持

![alt text](attachments/HMIAKVNT.png)

---
## 2.2 操作系统复杂性的管理方法——M.A.L.H方法
- **模块化（Modularity）**
  - 分治法：分解为通过明确接口交互的模块
  - 高内聚、低耦合
- **抽象（Abstraction）**
  - 接口与实现分离
  - 减少模块间交互，减少错误传播
- **分层（Layering）**
  - 模块化 + 抽象，强调层内完整性
  - 约束：只能与同层及相邻上下层交互，不跨层
  - 不同类模块间的层次化
- **层级（Hierarchy）**
  - 强调包含关系
  - 功能相近的模块组成自包含子系统
  - 同类模块间的分层

---

## 2.3 操作系统内核架构
### 重要性
多个应用程序需要共享硬件设备，需要一个统一管理所有硬件的内核
![\<img alt="" data-attachment-key="PDZVIYYT" width="718" height="357" src="attachments/PDZVIYYT.png" ztype="zimage"> | 718](attachments/PDZVIYYT.png)
### 2.3.1 简要结构
- 应用与操作系统在同一地址空间
- 无内存管理、无特权级隔离
- 适用于微控制单元等简单硬件
- 优点：快
- 缺点：无隔离，单点崩溃影响整体
- 典型：MS-DOS

### 2.3.2 宏内核
#### 结构
- 分层：内核层（特权级）+ 应用层（非特权级）
- 内核整合：文件系统、内存管理、设备驱动、进程调度、同步原语、IPC
- 典型：Linux
#### 优点
  - 丰富社区生态，经30年优化
#### 结构性缺陷
  - 安全与可靠性：模块间无强隔离，故障易扩散
  - 实时性：系统复杂，无法最坏情况时延分析
  - 庞大阻碍创新：Linux代码超2000万行
#### 难满足场景
  - 向上/向下扩展（KB级到TB级）
  - 硬件异构性
  - 功能安全（无法通过ASIL-D）
  - 信息安全（单点崩溃，大量CVE）
  - 确定性时延

### 2.3.3 微内核
#### 设计原则：最小化内核功能
- 操作系统功能移至用户态（服务/Server）
- 用户模块间通过消息传递（IPC）通信
![\<img alt="" data-attachment-key="CG6ESKS6" width="348" height="224" src="attachments/CG6ESKS6.png" ztype="zimage"> | 348](attachments/CG6ESKS6.png)
#### 发展
  - 第一代：Mach（1985），IPC通用但资源占用大
  - 第二代：L3/L4，最小化原则，性能极高，可能DoS
  - 第三代：seL4，Capability机制 + 形式化证明
#### 优点
  - 易扩展：直接添加一个用户进程即可为操作系统增加服务
  - 易移植：大部分模块与底层硬件无关
  - 更可靠：在内核模式运行的代码量大大减少
  - 更安全：即使存在漏洞，服务与服务之间存在进程粒度隔离
  - 更健壮：单个模块出现问题不会影响到系统整体
#### 缺点
  - 性能较差（函数调用 → IPC）
  - 生态欠缺
  - 重用宏内核兼容性带来新问题

### 2.3.4 混合内核
- 宏内核 + 微内核结合：性能模块放回内核态
- 典型
  - macOS/iOS：Mach微内核 + BSD 4.3 + 系统框架
  - Windows NT：微内核 + 内核态系统服务 + 系统框架

### 2.3.5 外核
#### 架构

**外核架构（Exokernel）+库（LibOS）**
*   **Exokernel 不提供硬件抽象**
    *   "只要内核提供抽象，就不能实现性能最大化"
    *   只有应用才知道最适合的抽象（end-to-end原则）
*   **Exokernel 不管理资源，只管理应用**
    *   负责将计算资源与应用的绑定，以及资源的回收
    *   保证多个应用之间的隔离
*   **Exokernel 把时间切片**
    *   在一个切片内，系统只服务一个应用，该应用可以访问所有硬件资源
*   **库OS（LibOS）**
    *   **策略与机制分离**：将对硬件的抽象以库的形式提供
    *   **高度定制化**：不同应用可使用不同的LibOS，或完全自定
    *   **更高性能**：LibOS 与应用其他代码之间通过函数调用直接交互
    *   **更安全可靠**：拥有特权的内核可以做到非常小，同时多个LibOS强隔离，提升安全性和可靠性

![\<img alt="" data-attachment-key="H9STNRXG" width="892" height="320" src="attachments/H9STNRXG.png" ztype="zimage"> | 892](attachments/H9STNRXG.png)

#### Exokernel 架构设计

*   **设计原则**：“将管理与保护分离”
*   **Exokernel 的功能**
    *   追踪计算资源的拥有权
    *   保证资源的保护
    *   回收对资源的访问权
*   **Exokernel 对应的技术**
    *   安全绑定
        *   将LibOS与计算资源绑定
            *   可用性：允许某个LibOS访问某些计算资源
            *   隔离性：防止这些计算资源被其他LibOS访问
    *   显式回收（Visible revocation）
        *   Exokernel 与应用之间的协议
            *   Exokernel 显式告知应用资源的分配情况
            *   应用在租期结束之前主动归还资源
    *   终止协议（Abort protocol）
        *   若应用不归还资源，则强制中止
            *   Exokernel 拥有对资源的控制权
            *   主动解除资源与应用间的绑定关系

#### 优点

*   OS无抽象，能在理论上提供最优性能
*   应用对计算有更精确的实时等控制
*   LibOS 在用户态更易调试，调试周期更短

#### 缺点

*   对计算资源的利用效率主要由应用决定
*   定制化过多，导致维护难度增加
*   可能不知道正在运行哪一种操作系统

![\<img alt="" data-attachment-key="5AEB8CK2" width="586" height="313" src="attachments/5AEB8CK2.png" ztype="zimage"> | 586](attachments/5AEB8CK2.png)
### 2.3.6 多内核
#### 架构
- 实验型架构，众核系统视为多核通过网络互联的分布式系统
- 节点间通过进程间通信交互
#### 设计思路
  - 每核运行独立小内核（OS node）
  - 应用运行在OS node之上
  - 节点间高速互连通信
#### 优点
  - 避免隐式共享带来的性能可扩展性瓶颈
  - 易支持异构处理器
#### 缺点
  - 节点间状态冗余，资源开销
  - 应用需使用多内核线程间通信接口，需移植
  - 绝对性能不一定有优势
#### 典型案例
  - Barrelfish（ETH/Zurich）：Dispatcher + Domain，支持CPU/ARM/GPU
  - Popsicle Linux：多ARM核异构，各核独立内核实例，上层统一接口

### 2.3.7 操作系统架构及演进
- 尽可能集成现有POSIX API/Linux ABI
- 避免棘手的系统调用（如fork）
- 避免不可扩展的POSIX API

---

## 2.4 操作系统框架结构（Android）
“身体是宏内核（Linux），思想是微内核（服务化）。”
### 分层架构设计
- **Linux内核层**：硬件驱动、进程管理、内存管理
- **硬件抽象层（HAL）**
  - 封装硬件实现细节，解耦内核与上层框架
  - 规避GPL协议（厂商可不开源核心驱动）
- **Android系统框架层**
  - Android库（自定义库 + 重写标准库）
  - Android运行时（ART/Dalvik）
    - Dalvik（旧）：JIT编译
    - ART（新，Android 5.0+）：AOT预编译，Java字节码预转机器码
- **应用框架层**：Activity Manager、Window Manager、Service Manager等

### 服务化架构与Binder IPC
- 体现微内核设计思想
- 解耦：组件服务化，App通过Binder IPC发起请求
- 交互流程
  1. 服务注册：系统服务在Service Manager注册
  2. 服务发现：App通过Service Manager查找服务句柄
  3. 服务请求：App通过Binder IPC发送请求并获取响应
- 优势：可扩展性、可维护性，单服务崩溃不影响整体内核

![\<img alt="" data-attachment-key="2CWKCKZV" width="457" height="328" src="attachments/2CWKCKZV.png" ztype="zimage"> | 457](attachments/2CWKCKZV.png)

### Dalvik vs ART
| 特性     | Dalvik（旧）          | ART（新）                |
| -------- | --------------------- | ------------------------ |
| 编译时机 | JIT（运行时即时编译） | AOT（安装时预编译）      |
| 运行速度 | 较慢，运行时开销      | 较快，直接执行二进制代码 |
| 系统能耗 | 较高（编译消耗CPU）   | 较低（减少重复编译）     |