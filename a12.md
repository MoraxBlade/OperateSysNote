# 12章 文件系统崩溃一致性
## 12.1 崩溃一致性基础概念
### 12.1.1 什么是文件系统崩溃
#### 常见崩溃场景：
- 突然断电、U盘热拔、数据线故障、硬件老化死机、系统蓝屏重启
#### 崩溃本质
- 内存中脏数据来不及同步到磁盘，磁盘元数据结构割裂不匹配，破坏系统不变量，造成文件丢失、损坏、分区无法挂载
### 12.1.2 文件系统一致性约束
#### 背景
- 文件系统多种数据结构存在依赖关系与一致性要求，必须满足不变量
#### 示例一致性要求
- inode文件大小与指向数据块总容量匹配
- inode硬链接计数与目录中指向该inode的目录项总数相等
- 超级块记录的总块、总inode数量与磁盘实际分配总量一致
- inode位图标记已占用的inode可通过目录遍历访问，空闲inode无文件引用
- 磁盘块不能同时处于空闲和文件占用状态
#### 崩溃破坏机制
- 文件修改由多步磁盘写入组成，崩溃发生在中间步骤，仅持久化部分修改，打破约束
### 12.1.3 崩溃一致性用户预期
#### 结构完整不变
- 磁盘元数据不破坏，重启后分区可正常挂载，无块双重占用、无效指针
#### 数据尽力持久
- 长期写入大文件完整保留，仅崩溃前少量未刷缓存修改丢失
#### 操作有序可见
- 操作遵循前缀有序，无顺序异常
### 12.1.4 磁盘故障模型：失效即停（fail-stop）
- 正常时严格执行读写指令，不篡改数据
- 崩溃时仅停止后续操作，不乱写磁盘
### 12.1.5 恢复方式分类
#### 离线恢复
- 关机后执行磁盘扫描修复
- 代表工具：Windows chkdsk、Linux fsck/e2fsck
- 缺点：大磁盘修复耗时极长；挂载状态下禁止执行
#### 在线恢复
- 系统运行时实时检测修复轻度不一致，无需卸载分区，性能损耗低
### 12.1.6 文件操作三大核心属性
#### 持久化Durable
- 操作完成后写入磁盘，重启后数据仍存在
#### 原子性Atomic
- 关联修改要么全部落地，要么完全不落地，无中间残缺状态
#### 有序性Ordered
- 操作按前缀可见，若B持久则其依赖的A一定已持久

## 12.2 三类主流崩溃一致性保障技术
### 12.2.1 日志机制（Journaling）
#### 核心思路
- 修改真实元数据前，先在独立日志区完整记录本次事务所有修改
- 日志完整写入提交标记后，再执行真实磁盘修改
#### 崩溃恢复逻辑：
1. 崩溃发生在日志提交前：日志不完整，直接丢弃，修改作废
2. 崩溃发生在日志提交后：重启读取日志，重放全部修改，保证事务原子性
#### 原始日志缺陷
1. 每次修改需两次磁盘写入（日志+真实数据），抵消页缓存优势
2. 数据需拷贝到日志，额外开销
3. 频繁同步IO，随机写性能差
#### 优化：内存日志+批量提交
- 日志先写入内存页缓存，异步批量刷盘，减少单次IO
- 合并多个文件操作到同一事务，同一块仅记录一次
#### 触发提交时机
1. 定时（5秒）
2. 日志容量达阈值
3. 应用主动调用fsync()
#### Linux JBD2日志子系统
- Ext3/Ext4通用日志驱动，管理磁盘事务
#### 核心概念
- Journal：磁盘独立日志分区/文件，存储事务记录
- Handle：单次原子操作对应的修改集合
- Transaction：批量合并多个Handle，统一提交磁盘
#### JBD2事务状态流转
1. 运行：可接收新原子操作
2. 锁定：停止接收新操作
3. 写入：事务数据写入存储设备
4. 提交：事务内容写入完成
5. 完成：事务生效，后台回收日志空间
#### JBD2基础接口
- 挂载阶段：初始化日志、加载历史事务用于恢复
- 系统调用处理：生成handle，标记待修改buffer为日志脏页
- 后台线程：定时自动提交事务，回收日志空间
- 卸载阶段：销毁日志资源，清空未完成事务
#### 磁盘结构
- 日志超级块：含魔法数字、块类型、块序号
- 事务：含描述块（tag数组记录日志块信息）、日志块
#### JBD2日志使用过程
1. 记录日志并提交，写入存储设备
2. 按日志记录将修改写回文件系统原位置
3. 删除日志，标记事务失效
#### Ext4三种日志模式（JBD2实现）
- Journal 完整日志模式：元数据+文件数据全部写入日志
- 一致性最强，崩溃无数据丢失；写放大严重，性能最低
- Ordered 顺序模式（Ext4默认）：仅日志记录元数据；数据先刷盘再提交元数据事务
- 保证元数据不指向未写入脏数据；平衡性能与一致性
- Writeback 写回模式：只记录元数据日志，文件数据无约束
- 速度最快；一致性最差，崩溃可能出现元数据存在、数据丢失
### 12.2.2 写时复制 Copy-on-Write CoW
#### 核心原理
- 修改数据时不直接覆盖，复制副本修改，递归向上更新所有上层索引、inode指针，直到根节点
- 多用于树状结构
#### 崩溃恢复性
- 仅完整根指针原子写入磁盘，修改才算生效
- 旧数据块保持不变，崩溃不会破坏原始文件结构
#### 文件多级索引CoW完整流程
1. 修改数据块→分配新数据块，拷贝旧内容并写入新数据
2. 一级索引块失效→分配新索引块，更新指针指向新数据块
3. 递归向上复制二级索引、inode块，逐层替换指针
4. 原子写入新inode，修改正式生效
5. 后台回收旧无效块资源
#### CoW优缺点
- 优点：天然支持原子更新，无需独立日志分区；原生支持快照、文件克隆；崩溃不会撕裂文件结构
- 缺点：存在写放大，微小修改复制整条索引链，随机写开销大
- 代表文件系统：Btrfs
#### CoW与日志对比（主观题考点）
1. 日志：需独立日志区，数据双写，恢复靠日志重放；随机写损耗适中，适合机械硬盘
2. CoW：无日志分区，修改全部分配新块，写放大更高；崩溃恢复简单，天然快照，适合SSD

## 12.3 日志结构文件系统 LFS（Log-structured FS）
### 12.3.1 设计背景与设计
#### 背景
- 文件读请求可被内存缓存处理，文件写成为瓶颈
- 机械硬盘随机寻道延迟高，顺序写性能远高于随机写
#### 核心设计
- 整个磁盘作为连续日志，所有inode、目录、数据、位图全部追加写入磁盘尾部，无原地覆盖修改
### 12.3.2 LFS数据结构
#### 固定位置的结构
- 超级块、检查点（checkpoint）区域
#### 以日志形式保存的结构
- inode、间接块、数据块；inode map；段概要；段使用表；目录修改日志
### 12.3.3 LFS使用举例（以创建文件为例）
- 新建/修改文件不覆盖旧块，仅在日志尾部追加全新inode、数据块、更新目录、更新inode map
- 旧版本数据自动失效，保留完整历史快照
### 12.3.4 段式垃圾回收（空间回收）
#### 问题背景：日志空间耗尽与无效数据堆积
- 物理容量有限，日志持续追加最终触及设备末端
- 文件修改删除后旧版本块占用空间，需回收重用
#### 空间回收的两种朴素方案及其局限
#### 方案一：串联空闲块（Free List / Linked List）
- 用链表串联无效块，需要时从链表分配
- 优点：实现简单
- 缺陷：反复分配释放后产生大量碎片，破坏顺序写优势，性能退化
#### 方案二：整体拷贝压缩（Copy Compaction）
- 读取所有有效数据，整理后拷贝到新区域，原区域标记可重用
- 优点：完全消除碎片
- 缺陷：搬运数据I/O开销大，需额外存储空间，不适合在线运行
#### 段式回收：两种方案的融合
- 以段为核心单位进行空间回收
#### 段的基本设计
- 设备拆分为定长区域（段），如512KB、1MB
- 每段内只能顺序写入；段内全为无效数据后才能重用
- 干净段用链表维护
#### 段使用表（Segment Usage Table）
| 字段         | 作用                                                             |
| ------------ | ---------------------------------------------------------------- |
| 有效字节数   | 记录段中仍被引用的有效数据总量；归零时段变为干净段，加入空闲链表 |
| 最近写入时间 | 记录段最后一次写入时间戳；用于组织非干净段，辅助清理策略决策     |
#### 段清理（Segment Cleaning）的完整流程
1. 读取待清理段到内存
2. 识别有效数据
3. 将有效数据整理写入干净段
4. 标记被清理段为干净
#### 段概要（识别有效数据）
#### 作用和结构
- 每个段附带段概要，记录每个数据块的inode编号、逻辑块序号、块类型
#### 有效性判定逻辑
- 根据(inode号, 逻辑块序号)查询inode当前最新指针
- 指针仍指向该物理块→有效；指针已变更或inode已删除→无效
### 12.3.5 检查点Checkpoint机制
#### 背景
- 全量扫描日志重建文件系统开销大，无效数据多
#### 内容
- inode map位置、段使用表、当前时间、最后写入段指针
#### 用法
- 定期将全局元数据写入双份检查点分区（双检查点避免写入崩溃）
- 仅需扫描检查点之后的日志段，大幅缩短挂载/恢复时间
### 12.3.6 崩溃恢复流程（前滚roll-forward）
#### 步骤
1. 读取最新有效检查点，加载全局inode映射
2. 遍历检查点后所有日志段，依据段概要重建文件结构
3. 目录修改日志修复inode硬链接计数
4. 丢弃孤立失效块
#### 目录修改日志
- 记录每个目录操作（create、link、rename、unlink）及具体信息
- 持久化在目录修改之前，恢复时保证inode链接数一致
### 12.3.7 典型LFS衍生文件系统
- 机械硬盘：WAFL；闪存设备：JFFS2、UBIFS、F2FS；非易失内存：NOVA

## 12.4 闪存友好型文件系统（F2FS）
### 12.4.0 闪存盘的组织
#### 通道（Channel）
- 控制器可同时访问的闪存芯片数量
#### 多通道（Multi-channel）
- 低端盘2或4通道，高端盘8或10通道
#### FTL闪存转换层
- 对外使用逻辑地址，内部使用物理地址
- 用于垃圾回收、数据迁移、磨损均衡
### 12.4.1 NAND闪存硬件特性
#### 读写擦除非对称
- 最小读写单位Page（8~16KB）；擦除单位Block（4~8MB）
#### 随机访问性能
- 无机械寻道，随机读优秀，但随机写仍弱于顺序写
#### 擦除循环
- 写入前需先擦除；每个块擦除次数有限
#### 通道
- 多通道，高并行性
#### 异质Cell
- SLC、MLC、TLC、QLC，存储1到4个比特
#### 磨损均衡
- 将写入操作均匀分摊在整个设备，避免局部写穿
### 12.4.2 传统LFS在闪存的缺陷
1. 单点日志写入，无法利用闪存多通道并行带宽
2. 修改元数据触发多层递归更新，写放大严重，加速闪存磨损
### 12.4.3 F2FS核心优化：NAT节点地址转换表
1. 新增NAT层隔离inode与物理磁盘块，inode仅记录Node逻辑编号，真实物理地址存NAT映射表
2. 修改文件数据仅更新少量NAT条目，无需递归复制整条索引链
3. 磁盘划分多组独立日志段，支持多流并行写入，利用多通道
### 12.4.4 F2FS磁盘分区布局
#### 组织层级
#### Block（块）：4KB，文件系统最小读写单位
#### Segment（段）：2MB，日志追加写入基础单位
#### Section（区）：多个Segment组成；F2FS垃圾回收操作粒度
#### Zone（分区/通道区）：多个Section组成；适配闪存多通道硬件，对应独立通道，可并行写入
#### 系统元数据（随机写入）
- 集中存放，局部性更好；含CP、SIT、NAT、SSA
#### 数据区（多Log顺序写入）
- 区分冷/温/热数据；区分文件数据段与元数据段

## 12.5 相关函数效果
### fsync
- 强制将文件所有脏元数据、脏数据同步刷入磁盘，阻塞等待完成；用于要求持久化的场景
### close
- 关闭文件描述符，仅标记缓存可回收，不保证数据立刻落盘
### lseek
- 仅修改进程内文件读写偏移，不操作磁盘持久化
### mmap
- 建立虚拟地址与文件映射关系，无同步刷盘能力，需搭配msync

## 12.6 核心主观题考点
### 题目1：日志与写时复制各自优缺点
#### 日志机制
优点：
1. 事务粒度可控，机械硬盘随机写损耗更小
2. 崩溃恢复流程简单，扫描日志即可重放事务
3. 实现成熟，主流文件系统广泛使用
缺点：
1. 双重写入（日志+真实数据），存在写放大
2. 独立日志分区占用固定磁盘空间
3. Writeback模式下文件数据无崩溃保护
#### 写时复制CoW
优点：
1. 天然原子修改，无日志分区开销
2. 原生支持快照、增量克隆
3. 崩溃不会破坏原有文件数据，无残缺中间状态
缺点：
1. 微小修改递归复制整条索引链，写放大更严重
2. 大量随机修改场景性能差，更适配SSD而非机械盘
### 题目3 JBD2三种日志模式区别
1. Journal：元数据+数据全写日志，安全性最高、性能最差
2. Ordered（默认）：仅日志存元数据，数据先落盘再提交事务，平衡安全与性能
3. Writeback：仅元数据日志，数据无保护，速度最快、断电易丢数据
### 题目4 新建文件崩溃8种场景分析
- 创建文件三步：标记inode占用→初始化inode→写入目录项，任意子集落地都会产生不一致

## 12.7 单选题核心知识点汇总
1. 保证缓存数据写入磁盘：fsync()
2. 崩溃一致性核心手段：事务原子更新
3. 违反一致性：同一块既空闲又被文件占用
4. Ext4最快日志模式：writeback写回模式
5. LFS核心优化：全部追加顺序写入磁盘
6. 闪存核心限制：擦除有寿命，读写擦除单位不同
7. Soft Updates最大优势：崩溃无需fsck，直接挂载