# 第5章 物理内存管理（精简提纲）
[TOC]
## 5.1管理物理内存资源（操作系统的职责）
### 5.1.1目标与评价维度
#### 目标
1. 提供易用物理内存抽象
    - 逐字节可寻址大数组
    - 屏蔽硬件细节
    - 简化OS物理内存管理
#### 物理内存管理碎片分类
1. **外部碎片**：空闲内存不连续，无法分配（分段机制典型问题）
2. **内部碎片**：分配块尺寸大于实际需求
#### 评价指标
1. **内存资源利用率**
    - 影响因素：外部碎片、内部碎片
    - 判定：碎片越少，利用率越高
2. **分配速度**
    - 复杂算法：碎片少、利用率高；查找计算耗时，分配慢
3. **核心权衡Tradeoff**：内存利用率与分配速度无法同时最优
### 5.1.2基于位图的连续物理页分配
#### 基于位图的物理内存池结构
1. 二进制位图数组管理物理页状态
    - 1位对应1物理页：0=空闲，1=已分配
    - 位图独立于真实内存空间
#### 初始化
1. 定义：`bit bitmap[N];` N=物理页总数，单页4KB
2. 初始化函数`init_allocator()`：全部位置0，初始全空闲
#### 分配n个连续的物理页
![\<img alt="" width="926" height="132" data-attachment-key="2IE99EWF" src="attachments/2IE99EWF.png" ztype="zimage"> | 926](attachments/2IE99EWF.png)
1. 核心代码逻辑
```c
u64 alloc_pages(u64 n) {
    int i, j, find;
    for (i = 0; i < N; ++i) {
        find = 1;
        for (j = 0; j < n; ++j) {
            if (bitmap[i+j] != 0) {
                find = 0;
                break;
            }
        }
        if (find) {
            for (j = i; j < i + n; ++j)
                bitmap[j] = 1;
            return FREE_MEM_START + i * 4K;
        }
    }
    return -1;
}
```
2. **执行步骤**
    - 遍历位图，检索连续n个0空闲页
    - 找到后批量置1标记已分配
    - 计算并返回物理地址；无连续页返回-1
#### 释放n个连续的物理页
1. 核心代码逻辑
```c
void free_pages(u64 addr, u64 n) {
    int page_idx;
    int i;
    page_idx = (addr - FREE_MEM_START) / 4K;
    for (i = 0; i < n; ++i)
        bitmap[page_idx + i] = 0;
}
```
2. **执行步骤**
    - 由物理地址换算起始页索引
    - 对应n个位批量置0标记空闲
    - 约束：释放地址、页数必须与分配匹配
#### 优缺点
1. 优点
    - 实现简单，易调试
    - 分配释放时间复杂度稳定O(N)
2. 缺点
    - 连续页查找遍历开销大，碎片多时性能差
    - 位图占用内存
    - 易产生外部碎片
### 5.1.3伙伴系统
#### 核心概念
1. **定义**：分配块尺寸固定为$2^k × 页大小$（页4KB，块4/8/16/32KB…）
2. 核心目标：分配连续内存，通过分裂/合并降低外部碎片
3. **核心规则**
    - 伙伴：同尺寸、地址相邻的空闲块，可合并为更大块
    - 伙伴判定：两块异或结果等于块大小；仅块对应位不同
    - 大块可分裂为两个同等大小伙伴块
#### 分配流程（需求15KB示例）
1. 需求向上取2的幂：15KB→16KB($2^2×4KB$)
2. 查找对应阶空闲链表
    - 存在直接分配
    - 无则向上取更大块分裂，一块分配、一块留存空闲链表
#### 伙伴判定计算示例
1. 步骤
    - 地址转二进制
    - 块大小$2^{13}$(8KB)，对比第14位
    - 两块地址异或值=块大小则为伙伴
#### 空闲块管理：链表数组
1. 数组下标对应阶数k，单下标挂载对应尺寸空闲块链表
    - k=0：4KB；k=1：8KB；k=2：16KB……
2. 分配逻辑：对应阶无块则向上分裂大块
#### 释放与合并流程
1. 释放块放回对应阶空闲链表
2. 校验伙伴块状态
    - 伙伴空闲：两块合并为大块，递归向上合并至无法合并
    - 伙伴已分配：释放流程终止
#### 优缺点总结
1. **优点**
    - 分配释放速度快，合并减少外部碎片
    - 链表数组结构简单，适配内核连续物理内存场景
2. **缺点**
    - 产生内部碎片（需求小于块尺寸时浪费）
    - 块尺寸限定2的幂，灵活性差
### 5.1.4SLAB分配器
#### SLAB分配器的背景与目标
1. 伙伴系统局限：最小单位4KB，分配内核小结构体产生严重内部碎片
2. **SLAB核心目标**：高速分配小对象，消除内部碎片
3. Linux SLAB家族
    - SLAB：原始版本，逻辑复杂
    - SLUB：简化版，Linux2.6.23后默认分配器
    - SLOB：极简实现，面向低内存设备
4. 发展历程
    - SLAB：Solaris2.4 Jeff Bonwick首创
    - SLUB：Christoph Lameter提出，Linux主流
    - SLOB：面向内存稀缺设备
#### 基本思想
1. 从伙伴系统获取整块连续内存
2. 切分为固定尺寸小块管理（多为$2^n$字节，3≤n<12；可自定义特殊尺寸）
3. 每种尺寸独立资源池，best fit快速匹配
#### SLUB 数据结构与资源池管理
1. 单尺寸资源池三类slab链表
    - current：当前优先分配slab
    - partial：存有空闲对象的未满slab链表
    - full：全部对象分配完毕的slab链表
2. slab内部：划分固定对象，空闲链表维护未分配对象
#### SLUB 的分配与释放流程
**分配流程**
1. 优先从current slab分配
2. current已满处理逻辑
    - partial链表取slab作为新current
    - 原current移入full链表
**释放流程**
1. 对象归还所属slab空闲链表
2. slab原在full链表则移至partial链表
3. slab完全空闲且partial为空，整块归还伙伴系统
#### SLUB 的优势与设计本质
1. 优势
    - 解决伙伴系统小对象内部碎片问题
    - 分配释放高效，适配内核高频固定对象分配
    - 结构简单，维护成本低于原生SLAB
2. 本质：伙伴系统上层二级分配器
### 5.1.5Linux内核内存管理架构
#### Linux架构分层
1. 用户空间：应用程序、用户态内存分配器
2. 内核空间：虚拟内存管理、物理内存分配器、内存控制组件
3. 硬件层：MMU、缓存、物理内存
#### 用户空间内存管理
1. 应用接口：malloc/free
2. 主流用户态分配器
    - ptmalloc：glibc默认，brk/mmap向内核申请页再拆分
    - tcmalloc：Google，多线程高性能
    - jemalloc：FreeBSD衍生，高并发场景适配
3. **核心特点**：用户分配器仅管理虚拟内存，物理内存映射分配由内核完成
#### 内核空间基本功能
**虚拟内存管理**
1. 系统调用接口
    - sys_brk：调整堆，分配释放小内存
    - sys_mmap：分配虚拟页映射区
    - sys_munmap：释放虚拟页
2. 延迟分配：首次访问虚拟页触发缺页异常，再分配物理内存
3. 核心组件
    - 缺页异常处理：分配物理页、更新映射
    - 页表管理：维护虚拟-物理地址映射关系
**物理内存分配器**
1. 页分配器：伙伴系统，以页为单位分配连续物理内存
2. 块分配器：SLUB/SLAB，内核小对象分配
3. 专用分配器
    - 不连续页分配器：vmalloc非连续物理页
    - 每处理器内存分配器：CPU本地对象缓存
    - CMA连续内存分配器：驱动预留连续内存，闲置供应用使用
**内存控制组件**
1. 内存碎片整理：降低外部碎片，提升大块连续内存分配成功率
2. 页回收：内存不足时回收冷页（文件页、匿名页）
3. OOM killer：内存耗尽、回收失效时终止高内存占用进程
4. 内存控制器组：限制进程内存资源，防止单进程耗尽内存
5. CMA：驱动预留连续空间，闲置供给应用
#### 物理页
1. struct page：单个物理页描述结构体
    - 采用联合体压缩结构体体积，减少内存占用
#### slab缓存
1. 数据结构定义文件：slub_def.h
## 5.2操作系统如何获得更多物理内存资源
### 5.2.1换页机制
#### 背景：物理内存超售与按需分配（原因）
1. 超售Over-commit：虚拟内存总申请量可远超物理内存容量
    - 场景1：多进程总需求超物理内存，依托虚拟内存+换页实现
    - 场景2：进程预分配大量虚拟内存，多数页面无访问，无需提前分配物理页
2. **按需分配**：虚拟页仅首次访问时分配物理页、建立映射
#### 虚拟页与物理页的共享映射
1. 多进程虚拟页可映射同一物理页（共享库、只读数据）
2. 提升内存利用率，消除数据冗余
#### 缺页异常（Page Fault）
1. **触发场景**
    - 虚拟页已分配，未绑定物理页（按需分配）
    - 虚拟页对应物理页已换出至磁盘
2. **处理逻辑**：分配/换入物理页，更新页表映射
#### 换页机制（Swapping）核心流程
1. **核心思想**
    - 冷物理页换出至磁盘Swap分区
    - 访问时换入物理内存
    - 虚拟内存容量不受物理内存限制
2. **实现载体**：磁盘Swap分区
3. **换出流程**
    - 筛选长期未访问物理页
    - 页面数据写入Swap分区
    - 回收物理页标记空闲
4. **换入流程**
    - 访问已换出虚拟页触发缺页异常
    - 从Swap读取页面数据
    - 写入空闲物理页，更新页表（不一定还在原来的位置）
#### 要点
1. **换入**：内存临近占满时提前执行，不等待内存耗尽
2. **换出**：页面换入物理页位置可与换出时不同
#### 按需分配的优缺点
1. **优势**：大幅节省物理内存，支持多进程并发运行
2. **劣势**：缺页异常引入磁盘I/O，访问延迟升高
#### 优化手段
1. **预取Prefetch**：缺页时批量换入相邻页，减少缺页次数
2. **LRU类换出策略**：优先淘汰最不常用的页，降低缺页概率
### 5.2.2页替换策略
#### 背景和目标
1. 触发条件：物理内存已满，发生缺页异常
2. 目标：最小缺页次数，减少磁盘I/O，提升系统性能
#### 最优替换算法（OPT/MIN）
1. **思路**：淘汰未来最长时间不访问/永不访问的页面
2. **特点**：理论缺页率最低；无法落地，无法预知未来访问序列
#### 先进先出（FIFO）
1. **思路**：队列记录页面载入顺序，淘汰最早进入内存页面
2. **特点**：实现简单，无需记录访问标记；存在Belady异常（内存帧数增加，缺页次数上升）
#### FIFO改进版：second chance策略
1. **思路**
    - 页面访问添加访问标记
    - 换页时队头有标记则清除标记移至队尾；无标记直接淘汰
2. **特点**
    - 大幅缓解Belady异常，缺页率优于原生FIFO
    - 仅需1位标记，开销低
    - 近似LRU性能，常用页面留存内存
#### ⭐最近最少使用（LRU）【面试高频】
1. **思路**：淘汰过去最久未访问页面，依托时间局部性；双向链表维护，新访问页面移至链表尾部，淘汰头部
2. **特点**：性能接近OPT；实现开销高，需记录页面访问时间
3. 实现方案：哈希表+双向链表
#### 最近最常使用（MRU）
1. **思路**：淘汰近期刚访问页面，假设短期不会重复访问
2. **特点**：实现简单，LRU反向策略；适配单次访问地址序列场景
#### 最近未使用（NRU）/时钟算法（Clock）
1. **思路**：访问位+修改位近似模拟LRU；定期清零访问位；循环链表+时钟指针，优先淘汰访问位为0页面
2. **特点**：性能接近LRU，实现成本低；Linux主流采用
#### 最常使用（MFU）与最不常使用（LFU）
1. MFU：淘汰累计访问次数最多页面
2. LFU：淘汰累计访问次数最少页面
3. 缺陷：LFU对新页面不公平，无法适配动态变化的访问模式
#### 颠簸（Thrashing）现象
1. 成因：内存不足/页替换策略不合理，页面频繁换入换出
2. 表现
    - 系统资源大量消耗在缺页异常与磁盘I/O
    - CPU利用率骤降，业务进程停滞
3. 连锁危害
    - 磁盘I/O阻塞导致CPU利用率下降
    - 调度器加载更多进程，加剧缺页异常
#### 机器学习辅助的动态替换策略
1. 背景：传统静态算法无法适配复杂多变访问负载
2. **思路**：强化学习/多臂赌博机预测页面访问规律，动态切换替换策略
3. 优势：复杂负载下缺页率优于固定策略