# 13章 设备管理
## 13.1 设备连接
### 13.1.1 外设多样性的挑战
#### 背景
- 外设种类繁多，各有协议规范
#### 核心挑战
- 如何标准化外设接口
- 如何获知外设状态并修复错误
### 13.1.2 外设的认知维度
1. 基本用途：设备提供的功能
2. 共同特征：能否分类简化抽象
3. 编程接口：设备可编程接口形式
### 13.1.3 硬件接口的物理基础
#### GPIO（通用输入输出）
**矛盾**：硬件接口管脚数目有限，外接设备数量多
**作用**：低成本拓展硬件控制能力，解决接口数量不足
**结构**（GPIO LED）：通过INPUT/OUTPUT管脚控制，01组合表示状态
#### Intel 8042（PS/2键盘控制器）
- 电信号→数字信号→编码（Scan Code）
- 每次只能键入一个字符；长按时重复产生按键事件
#### UART（串口）
- 通用异步收发传输器；半双工通信
- 每次只能传输一个字符
#### Flash闪存
- 以页/块为粒度读写/擦除；支持页/块随机访问
#### Ethernet网卡
- 每次传输一块数据（以太网帧），有严格结构定义
- WiFi、蓝牙原理类似
#### 其他设备
- 显示器、传感器、协处理器等；统一管理方式为抽象

## 13.2 设备类型抽象
### 13.2.1 抽象的复用：文件系统接口
- 操作系统封装外设细节，提供统一抽象接口
- 统一open/read/write/close操作，底层驱动处理设备差异
### 13.2.2 设备抽象：以Linux为例
| 设备类型                   | 代表设备                 | 访问模式             | 抽象方式                          |
| -------------------------- | ------------------------ | -------------------- | --------------------------------- |
| 字符设备（Char Device）    | 键盘、鼠标、串口、打印机 | 顺序访问，逐字符读写 | 文件抽象（open/read/write/close） |
| 块设备（Block Device）     | 磁盘、U盘、闪存          | 随机访问，以块为粒度 | 内存抽象（mmap）、文件抽象        |
| 网络设备（Network Device） | 以太网、WiFi、蓝牙       | 格式化报文收发       | 套接字（Socket）抽象              |
### 13.2.3 字符设备（Char Device）
#### 例子
- 鼠标、键盘、串口、打印机等
#### 访问模式：
- 顺序访问，每次读取一个字符；驱动直接与设备交互
#### 抽象
- 通常使用文件抽象：open()、read()、write()、close()
#### 示例：通过字符设备实现终端间通信
- 向终端设备文件写入数据，对应终端可接收消息
### 13.2.4 块设备（Block Device）
#### 例子
- 磁盘、U盘、闪存等存储设备
#### 访问模式
- 随机访问，以块粒度读写；驱动之上增加缓冲，避免频繁交互
#### 抽象
- 通常使用内存抽象（mmap），也可使用文件抽象
#### 示例：查看系统中的块设备
- /proc/devices查看主设备号；lsblk列出块设备及挂载点；fdisk查看分区信息
### 13.2.5 网络设备（Network Device）
#### 例子
- 以太网、WiFi、蓝牙等通信设备
#### 访问模式
- 面向格式化报文收发；驱动层之上维护协议栈，支持不同策略
#### 抽象
- 通常使用套接字（Socket）抽象：提供socket、bind、listen、accept、send/recv等接口
### 13.2.6 设备类型判断示例
- UART串口→字符设备；SD卡→块设备；以太网→网络设备；GPIO→字符设备

## 13.3 设备与操作系统的交互
### 13.3.0 两种交互模式
#### 可编程 I/O（Programmable I/O）
- 通过CPU in/out或load/store指令
- 消耗CPU时钟周期与数据量成正比
- 适合简单小型设备
#### 直接内存访问（Direct Memory Access, DMA）
- 外设直接访问总线，与内存传输数据无需CPU参与
- 适合高吞吐量I/O
### 13.3.1 PIO
#### PIO的两种实现方式
| 方式                      | 说明                   | 指令                             |
| ------------------------- | ---------------------- | -------------------------------- |
| Port I/O                  | 设备具有独立地址空间   | x86的in/out指令                  |
| MMIO（Memory-mapped I/O） | 设备映射到连续物理内存 | 普通内存访问指令（读写有副作用） |
#### MMIO代码示例（ARM汇编）：
- 通过读写设备寄存器完成硬件初始化
#### CPU与外设的数据交互
- 外设通过PCI总线通信；设备唯一标识：总线号:设备号.功能号
- lspci命令查看PCI设备
#### PIO的设备交互的三种数据流动方向
1. 数据从内存读到寄存器
2. 数据从CPU写到外设
3. 数据从外设写入CPU
#### 问题：频繁访问场景CPU开销大→引出DMA
### 13.3.2 直接内存访问（DMA）
#### 核心思想
- 外设绕开处理器直接存取物理内存，释放CPU处理其他任务
#### DMA工作流程
1. CPU向DMA控制器发送请求（方向、内存位置、大小）
2. 外设直接与内存传输数据（无需CPU参与）
3. DMA传输完成后通知CPU
#### DMA的特点
- 设备地址空间特殊，需地址转换
- 数据传输无需CPU参与，适合高吞吐量场景
#### DMA的传输顺序示例
- 硬盘读内存顺序：初始化DMA并启动硬盘→硬盘传输数据到内存缓冲区→DMA发中断请求→执行中断服务程序

## 13.4 操作系统如何响应设备：中断
### 13.4.1 中断的分类（AArch64）
#### IRQ (Interrupt Request)
- 普通中断，优先级低，处理慢
#### FIQ (Fast Interrupt Request)
- 快速中断，优先级高，处理快；一次只能有一个FIQ
#### SError (System Error)
- 异步中止导致的系统错误异常，原因难定位
### 13.4.2 中断控制器
#### 主要功能：
- 分发：管理所有中断、决定优先级、路由
- CPU接口：为每个核心提供对应接口
#### 需要考虑的问题：
- 中断优先级指定；中断嵌套；中断分发；与软件协同
### 13.4.3 查看中断状态
#### 命令
- /proc/interrupts查看各核心中断统计
### 13.4.4 中断优先级
#### 规则
- 多中断同时发生时，优先响应高优先级中断
#### 分类
- 复位（-3）> 不可屏蔽中断NMI（-2）> 硬件故障（-1）> 可配置类中断
### 13.4.5 中断嵌套
- 高优先级中断可抢占低优先级中断处理
- 同级中断无法抢占
- FIQ可抢占任意IRQ，自身不可被抢占
### 13.4.6 硬中断
#### 中断的问题
- 抢占用户任务，影响实时性
- 处理中断时屏蔽当前IRQ，缓冲区不足会导致中断丢失
- 中断处理必须尽可能短
#### 中断处理程函数ISR：
- request_irq注册中断处理函数
#### 处理
- ISR仅做关键操作：移出缓冲区数据、标记flag
- 非关键操作推迟到下半部完成
#### Linux中断的上下半部（Top Half / Bottom Half）
**设计思想**
- 将中断处理分为两部分，平衡响应速度和系统开销
**上半部（Top Half）：马上做（硬）**
- 执行硬中断处理函数；中断屏蔽下快速响应，最小化屏蔽时间
**下半部（Bottom Half）：延迟完成（软）**
- 处理繁重非关键操作；可被中断
- 实现方式：软中断（Softirqs）、Tasklet、工作队列（Work Queue）
### 13.4.7 软中断（Softirqs）
#### 静态分配
- 内核编译时确定，数量有限；按优先级排序
#### Softirqs的特点
- 执行时间点：中断之后、系统调用/异常之后、ksoftirqd进程显式执行
- 并发性：可在多核同时执行，需可重入或加锁
- 可中断性：运行时可被中断抢占
#### 可重入函数
- 不可重入：依赖全局变量，多线程调用结果不一致
- 可重入：参数传入数据，多线程调用结果一致
#### Tasklet
**动机**
- Softirq静态分配，无法动态创建销毁
**Tasklet的特点**
- 基于Softirqs实现；可动态创建销毁
- 同一类型Tasklet一次只能运行一个；不同类型可在不同CPU同时运行
#### 工作队列（Work Queue）
**特点**
- 使用进程上下文，可以睡眠
**工作方式：**
- 内核空间维护FIFO队列，workqueue进程轮询执行
- 中断负责入队，workqueue负责出队执行
**优势：**
- 仅在内核空间运行，不关联用户进程；无跨模式切换和拷贝开销

## 13.5 操作系统如何管理设备
### 13.5.1 驱动程序（Driver）
#### 定义
- 使操作系统与设备相互通信的特殊程序
#### 宏内核 vs. 微内核的驱动
| 架构                  | 驱动位置 | 优势     | 劣势                           |
| --------------------- | -------- | -------- | ------------------------------ |
| 宏内核（Monolithic）  | 内核态   | 性能更好 | 容错性差，驱动崩溃导致系统崩溃 |
| 微内核（Microkernel） | 用户态   | 可靠性好 | 性能开销（IPC通信）            |
#### 驱动的系统接口：ioctl
- 用户空间应用通过驱动间接与设备交互，支持多种命令分支
#### 驱动模型
- 驱动在系统统一安排下与设备交互
#### 交互链
应用程序 → 系统接口 → 驱动模型 → 驱动程序 → 设备
### 13.5.2 驱动模型（LDDM）
#### 为什么需要驱动模型？
- 设备数量规模增大、更新加快，驱动代码量增长
- 需标准化数据结构和接口，简化驱动开发
#### LDDM的核心功能
- 支持电源管理与热插拔
- 通过sysfs向用户空间提供系统信息
- 维护驱动对象依赖关系与生命周期
#### LDDM的三层抽象
**struct bus_type：对设备连接关系的抽象**
- 总线名称；匹配设备与驱动；上报热插拔事件；probe/remove等回调
**struct device：对硬件设备的抽象**
- 父设备、设备名、所属总线、对应驱动、设备号、驱动私有数据等
**struct device_driver：对设备驱动的抽象**
- 驱动名称、所属总线、probe/remove回调、电源管理接口
#### LDDM的工作流程
1. 设备与驱动都注册到总线
2. 总线match()匹配设备与驱动
3. 匹配成功调用驱动probe()
4. probe成功后内核生成设备实例
5. 驱动注册的接口可被应用访问

## 13.6 设备树（Device Tree）
### 13.6.1 设备树的由来
#### 问题：如何描述硬件设备信息
- 在内核编码硬件细节冗余且不灵活
#### 设备树（Device Tree）的解决方案：
- 定义：描述硬件的数据结构
- 通过DTS（设备树源文件）将硬件信息传递给内核，避免内核编码硬件细节
#### 模型
- 设备树描述硬件拓扑，内核根据设备树加载对应驱动
### 13.6.2 DTS语法
- 根节点、子节点结构；节点名@地址格式
- 属性类型：字符串、数值数组、二进制数据、节点引用
- 标签与引用语法
#### DTS语法要点：
| 语法                        | 说明                  |
| --------------------------- | --------------------- |
| 节点名@地址                 | 设备节点定义          |
| property = "string"         | 字符串属性            |
| property = <1 2 3>          | 数值/引用属性（多值） |
| property = [0x01 0x02 0x03] | 二进制数据属性        |
| &node1                      | 节点引用              |
| node1: node@1               | 带标签的节点          |
### 13.6.3 驱动对DTS的使用
#### 匹配
- 通过compatible属性进行设备与驱动的匹配
#### 加载
- 驱动以模块形式动态加载；module_init/module_exit指定入口出口
