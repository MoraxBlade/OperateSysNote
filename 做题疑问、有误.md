10. 哪种映射方式会在首次访问时触发缺页异常？
---

13. 步幅调度

---
16. 下列调度算法中，不可能导致饥饿现象的是
A 时间片轮转
B 静态优先级调度
C 非抢占式最短完成时间任务优先
D 抢占式最短完成时间任务优先


---
29. Intel DPDK的核心思想是
A 强化内核协议栈处理能力
B 数据面与控制面分离，绕过内核直接在用户空间处理数据包
C 增加中断频率提高响应速度
D 使用传统多线程技术优化性能
>答案：B
>解析
>1. **A错误**：DPDK不强化内核协议栈，反而绕开Linux内核网络协议栈，规避系统调用、中断开销。
>2. **B正确**：DPDK核心思路是数据面/控制面分离，通过UIO/VFIO驱动把网卡报文收发放到用户态，轮询收发包，消除内核损耗。
>3. **C错误**：DPDK采用轮询机制，完全关闭网卡中断，不会增加中断频率。
>4. **D错误**：DPDK采用亲和绑定、无锁批量处理，并非依赖传统多线程优化性能。


---
30. 操作系统安全的三个层次不包括
A 基于OS的应用隔离与访问控制
B OS对恶意应用的隔离与防御
C 硬件级内存加密保护
D OS不可信时对应用的保护
>答案：C
>解析
>操作系统安全经典三层划分：
>1. **第一层（A）**：基于OS的应用隔离与访问控制（OS可信，正常权限隔离、用户/内核隔离）
>2. **第二层（B）**：OS对恶意应用的隔离与防御（OS可信，拦截恶意程序越权行为）
>3. **第三层（D）**：OS不可信时对应用的保护（如TEE、TrustZone，OS被攻破也能保护应用数据）
---
31. 操作系统最主要的功能是什么？