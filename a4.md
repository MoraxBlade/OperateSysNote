## 4章 虚拟内存管理

* **核心**
  * CPU支持虚拟地址空间
  * OS配置并使能虚拟内存机制
  * 所有软件使用虚拟地址，无法直接访问物理地址
* **虚拟地址**：程序使用虚拟地址访问主存；每个应用程序拥有独立的虚拟地址空间

## 4.1 CPU的职责：内存地址翻译

### 4.1.1 地址翻译（设备与步骤）

#### MMU 内存管理单元（Memory Management Unit）
- **职责**：虚拟地址 → 物理地址转换
- 翻译规则取决于分段/分页机制

#### TLB 转址旁路缓存（Translation Lookaside Buffer）
- 加速地址翻译的硬件缓存单元（MMU内部），缓存虚拟页号→物理页号映射

### 4.1.2 分段机制

- **定义**：虚拟地址空间和物理内存按“段”（连续物理内存块，大小可变）管理
- **虚拟地址结构**：段号 + 段内偏移
- **段表**：存储段号、段起始物理地址、段长
- **翻译过程**：段表基址寄存器 → 段表项（起始物理地址） + 偏移 → 物理地址
- **缺陷**：外部碎片、交换效率低

### 4.1.3 分页机制

- **基本思想**：虚拟空间划分为等长虚拟页；物理内存划分为等长物理页；页长固定且相等
- **页表**：OS为每个应用构造的映射表（虚拟页号→物理页号）；页表基地址寄存器（TTBR）存储页表起始地址
- **虚拟地址结构**：虚拟页号 + 页内偏移
- **翻译过程**：解析虚拟页号 → 页表查找物理页号 → 物理页起始地址 + 偏移
- **优势**：离散分配、连续虚拟地址空间、进程隔离；切换应用时切换TTBR

### 4.1.4 多级页表

- **单级页表问题**：$\text{页表大小} = \frac{\text{总虚拟地址空间}}{\text{页面大小}} \times \text{每个 PTE 的大小}$
  - 即使大部分地址未使用也需维护全部表项，浪费空间
- **AArch64虚拟地址结构（48位）** ：高16位固定（全0/全1）；虚拟页号36位，平均分4份（每份9位）；每级页表最多512项（2⁹）；页内偏移12位
- **查询翻译流程**：
  1. TTBR0_EL1/TTBR1_EL1（根据第63位选择）→ L0页表基地址
  2. L0_INDEX → L0页表项 → L1页表基地址
  3. L1_INDEX → L1页表项 → L2页表基地址
  4. L2_INDEX → L2页表项 → L3页表基地址
  5. L3_INDEX → L3页表项 → 物理页号
  6. 物理页号 + 偏移 → 物理地址
- **优势**：节省内存（仅为使用的地址空间分配页表）；无需连续物理内存；支持大虚拟空间；精细权限控制
- **缺陷**：若用满虚拟地址空间，多级页表可能占用更多内存
  - 反例（Intel PAE 32位，用满4GB）：多级页表 = 8MB + 20KB；单级页表 = 8MB

### 4.1.5 页表项与大页（AArch64）

#### 多级页表和大页映射
- **大页**：中间级页表项（L0/L1/L2）直接指向物理页，而非指向下一级页表
- **映射大小**：在某一级终止翻译，后续索引位全部作为页内偏移 → 页大小 = 2^（后续位数）
  - L0：512GB大页 或 → L1
  - L1：1GB大页 或 → L2
  - L2：2MB大页 或 → L3
  - L3：仅4KB标准页
- **类型位**：第1位，1=表描述符/块描述符；0=页描述符
- **PFN**：物理页号

#### 页描述符（指向4KB页，L3页表项）
- **Upper attributes**：XN/UXN（用户态执行权限），PXN（内核态执行权限），DBM（dirty位）
- **Lower attributes**：AF（访问标志），Shareability，AP（读写权限），AttrIndx（内存类型）

#### 块描述符（指向大页）
- 共享UXN/PXN/AF/AP属性；PFN段更长

#### 异常处理与地址翻译
- 缺页异常（V=0）、访问权限异常、访问标志位异常、权限执行异常
- AArch64：8号同步异常，ESR/FAR_EL1；x86-64：14号异常（#PF），CR2

#### 页表使能
- AArch64：SCTLR_EL1第0位（M位）置1
- x86-64：CR0第31位（PG位）置1

#### 大页映射的优缺点
- **优点**：减少页表数量、减少TLB缺失
- **缺点**：内部碎片、换页粒度粗、一次翻译可能访问多级页表耗时长

### 4.1.6 TLB：页表的缓存（翻译加速器）

- **TLB**：缓存虚拟页号↔物理页号映射（键值对哈希表）
- **作用**：减少多级页表地址翻译中的访存次数
- **TLB命中**：直接获取物理页号；**TLB未命中**：遍历页表
- **结构**：L1数据TLB、L1指令TLB；L2统一（或分离）
- **TLB刷新**：切换页表时需刷新（不同进程相同虚拟地址映射不同物理地址）
- **降低刷新开销**：
  - AArch64：内核和应用使用不同页表（TTBR0_EL1/TTBR1_EL1），系统调用不切换；ASID区分进程，进程切换不刷新全部TLB
  - x86-64：内核映射到应用页表高地址，系统调用不切换CR3；PCID区分进程

## 4.2 管理页表映射（操作系统的职责）

### 4.2.1 直接映射（操作系统为自己配置页表）

- **原因**：CPU上电默认物理寻址；OS初始化使能MMU后，所有地址均为虚拟地址；OS需为自身配置页表
- **特点**：
  - AArch64：OS使用高16位为1的虚拟地址；应用使用高16位为0的虚拟地址
  - 一次性映射全部物理内存；采用直接映射（虚拟地址 = 物理地址 + 固定偏移）
- **机制**：MMU保存固定偏移量，与页内偏移拼接得到物理地址；OS读写页表时使用虚拟地址，依靠内核直接映射完成地址再翻译

### 4.2.2 虚拟内存段分布（如何填写进程页表）

- **内存区域**：虚拟内存起始/终止地址、区域权限、文件偏移、动态加载代码库等
- **ELF可执行文件**：
  - 组成：ELF头部、多个程序段（连续二进制块）、可选的段头表
  - 常见段：.text（代码段）、.data（数据段）、.rodata（只读数据段）、.bss（零数据段，文件内不占空间）
- **程序加载与执行**：加载（按Load Memory Address拷贝到内存）→执行（按Virtual Memory Address放置段）
- **内存分布**：低地址→代码段、数据段、堆（自低向高增长）、代码库、栈（自高向低增长）

### 4.2.3 进程页表配置与管理

- **核心机制**：每个进程拥有独立页表；页表基地址和页表项存储物理地址；OS读写时需虚拟地址再翻译；多级页表按需分配下级页表
- **统一页表框架（Linux）** ：L0（PGD）、L1（PUD）、L2（PMD）、L3（PT）；Linux 4.11新增P4D
- **核心数据结构**：
  - `struct process`：包含页表基地址 `pgtbl`（物理地址）
  - `struct vmspace`：包含 `pgtbl` 和 `vmregions` 链表
  - `struct vmregion`：表示单个虚拟内存区域（start、end、perm）
- **核心接口**：
  - `add_mapping(pgtbl, va, pa)`：添加va→pa映射
  - `delete_mapping(pgtbl, va)`：删除映射
- **获取下级页表函数 `get_next_pgtbl_page`**：
  - 入参：当前页表基地址、索引；若页表项为空则分配新物理页并填充；返回时需执行物理地址→内核虚拟地址再翻译
- **添加映射流程**：L0基地址 → L0_INDEX → 获取L1 → L1_INDEX → 获取L2 → L2_INDEX → 获取L3 → L3_INDEX → 写入物理地址+权限位
- **删除映射流程**：逐级查找定位L3页表项 → 清空 → `flush_tlb` 精准刷新
- **进程生命周期**：
  - 创建：加载ELF和动态库，填写初始页表
  - 执行：栈/堆变化，mmap/munmap修改虚拟内存区域
  - 退出：删除整个页表

### 4.2.4 管理页表映射：立即映射

- **mmap接口**：`void *mmap(addr, length, prot, flags, fd, offset)`
- **立即映射策略**：虚拟内存区域声明后，立即分配物理内存并填写页表（`alloc_page` → `add_mapping`）
- **映射策略**：声明后立即分配物理内存并填写页表映射
- **优点**：逻辑简单直观
- **缺点**：启动时延增加（加载所有内容）；内存资源浪费（mmap大区域但未实际使用）

### 4.2.5 延迟映射

- **核心逻辑**：先声明内存区域，待实际访问时才分配物理内存并填写页表
- **缺页处理流程**：
  1. 异常地址按页大小向下取整
  2. 遍历当前进程的`vmregions`链表，检查地址合法性和访问权限
  3. 分配物理页 `alloc_page()` → `add_mapping`
- **实现**：`mmap`仅新增`vmregion`结构体，不分配物理页
- **优点**：节约物理内存
- **缺点**：首次访问触发缺页异常，降低性能
- **Linux实现**：
  - `mm_struct`：描述进程虚拟地址空间（含`pgd`、VMA链表、VMA红黑树）
  - VMA通过红黑树组织，高效查找
  - 缺页异常处理链：`do_page_fault` → `handle_pte_fault` → 匿名/文件映射处理
- **段错误**：访问地址不属于合法虚拟内存区域时，OS判定为非法访问，报告Segmentation Fault

### 4.2.6 虚拟内存的扩展功能

#### 共享内存、写时拷贝
- 修改页表权限为只读；写触发缺页异常时拷贝物理页并恢复写权限
- 典型场景：`fork()`，父子进程共享物理内存，仅写时复制
- 流程：多进程共享 → 某进程写触发权限异常 → 分配新物理页复制数据 → 更新该进程页表并恢复写权限

#### 内存去重
- OS扫描合并内容相同的物理页，以只读方式共享；写入时触发写时拷贝分离
- 典型案例：Linux KSM
- 安全隐患：侧信道攻击

#### 内存压缩
- 内存紧张时压缩低频使用页，释放物理内存
- Windows：压缩数据留存内存，访问时即时解压
- Linux zswap：压缩待换出数据存入zswap缓存，减少磁盘读写

#### 大页
- **原理**：高阶页表项直接指向大容量物理页，减少页表层级与TLB占用
- **计算**：
  - L2页表项第1位=0 → 跳过L3，偏移 = 12位（页内）+ 9位（L3索引）= 21位 → 2MB
  - L1页表项第1位=0 → 跳过L2+L3，偏移 = 12 + 9 + 9 = 30位 → 1GB
- **优点**：降低地址翻译开销，提升TLB命中率
- **缺点**：内部碎片，管理复杂度增加
- **实现**：应用显式分配API；Linux透明大页自动合并