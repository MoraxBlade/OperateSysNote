好的，收到。我已根据你提供的全部课程PDF讲义（涵盖第1章至第17周），在原有汇总的基础上，**按章节（二级标题）和题型（三级标题）** 重新进行了全面扩充和补充。此次新增了大量PDF中出现的选择/填空/分析题原型，并给出了标准解题方法。

---

## 第1章：操作系统概述
### 题型1：操作系统定义与服务（概念辨析）
- **考查方式**：问“如果一台机器只有一个专用应用程序，是否需要操作系统？”或“操作系统 = 管理 + 服务，两者目标有何冲突？”
- **解题方法**：
  - **是否需要OS**：即使只有一个应用，若需管理硬件资源（CPU时间片、内存隔离）、提供系统调用（如`write`），**需要**操作系统；若应用完全裸跑并自己管理硬件，**不需要**，但极少见。
  - **管理 vs 服务冲突**：服务追求单个应用运行效率最大化（如独占CPU），管理追求整体资源利用率最大化（如公平调度引发频繁上下文切换，牺牲单任务性能）。
- **典型答案**：单选题答案选 **A（需要）**；管理和服务目标存在天然矛盾。

---

## 第2章：操作系统结构
### 题型1：内核架构归类（填空题/选择题）
- **考查方式**：给出系统名称，问属于哪种内核架构（宏内核、微内核、混合内核）。
- **解题方法**：
  - **Linux**：宏内核（Monolithic）。
  - **Windows NT**：混合内核（Hybrid，微内核+内核态系统服务）。
  - **macOS / iOS**：混合内核（XNU = Mach微内核 + BSD）。
  - **Android**：身体是宏内核（Linux），思想是微内核（服务化+Binder IPC）。
- **典型答案**：Windows NT -> 混合内核；Linux -> 宏内核；macOS -> 混合内核。

### 题型2：策略（Policy）与机制（Mechanism）分离举例
- **考查方式**：调度策略（如Round-Robin）属于哪个？调度队列实现属于哪个？
- **方法**：**策略 = 做什么**（如优先级高低、调度算法）；**机制 = 怎么做**（如运行队列、中断处理）。
- **答案**：调度算法 -> 策略；运行队列（rq）、上下文切换 -> 机制。

---

## 第3章：硬件环境与软件抽象（ARM汇编与栈帧）
### 题型1：条件分支与状态寄存器（PSTATE）
- **考查方式**：给定汇编代码（`subs w1, w1, #1; bne .L3`），问跳转条件。
- **方法**：
  - `subs` 更新状态位（N/Z/C/V）。
  - `bne` 依赖 `Z` 位：`Z=0`（结果非零）时跳转。
- **答案**：`w1` 减1后不为0则跳转至 `.L3`。

### 题型2：函数调用栈帧（画图题，已补充）
- **补充细节**：除了 `main` 和 `comb`，注意**叶子函数**（如 `square`）可能不保存 `FP/LR`，栈帧大小为0或16字节。
- **方法**：看汇编开头的 `sub sp, sp, #XX` 确定栈帧大小。

### 题型3：寻址方式（前变址/后变址）
- **补充考查**：`LDR X0, [X1, #8]!` 与 `LDR X0, [X1], #8` 执行后 `X1` 的值有何不同？
- **答案**：前者先 `X1=X1+8` 再访存；后者先访存再 `X1=X1+8`。

---

## 第4章：虚拟内存管理（核心计算密集区，已包含你提供的新题）
### 题型1：多级页表内存开销（补充新题）
- **补充考查（来自PDF）**：若只用3级页表（36位页号均分3等份，每份12位，页内偏移12位），问L1页表项直接映射（跳过L2）的大页大小为多少？
- **方法**：跳过L2（12位索引），总偏移 = 12（页内）+ 12（L2索引）= 24位 -> $2^{24}$ 字节 = **16MB**。
- **答案**：**16MB**。

### 题型2：地址翻译（VA→PA）与页表项结构
- **补充考查**：页表项中 `AF`（Access Flag）位为0时访问会发生什么？`XN` 位的作用？
- **方法**：
  - `AF=0`：访问时触发**缺页异常**（供操作系统追踪访问情况）。
  - `XN`（Execute Never）：禁止用户态（EL0）执行该页代码。
- **答案**：触发异常；禁止用户态执行。

### 题型3：大页（Huge Page）优缺点
- **补充考查**：大页的优点和缺点分别是什么？
- **优点**：减少TLB miss，减少页表遍历级数。
- **缺点**：内部碎片增大（未使用完整大页），内存管理复杂度增加。

---

## 第5章：物理内存管理与页置换
### 题型1：伙伴系统（Buddy System）伙伴判定（新题）
- **考查方式（PDF真题）**：两个8KB空闲块首地址为 `0x0000 2425 8ACF 2000` 和 `0x0000 2425 8ACF 4000`，问是否互为伙伴？
- **方法**：
  1. 两个地址差 = `0x2000 = 8192 = 8KB`，且大小同为8KB。
  2. 检查地址是否按 `2 * 块大小 = 16KB` 对齐。`0x...2000` 对16KB取模为0，`0x...4000` 对16KB取模为0。
  3. 异或 `0x2000 ^ 0x4000 = 0x2000`，等于块大小。
- **答案**：**互为伙伴**。

### 题型2：页置换算法追踪（FIFO / LRU / OPT / Clock）
| 中文名称                | 英文缩写/别名 | 淘汰原则                                                                        | 关键特点                               |
| :---------------------- | :------------ | :------------------------------------------------------------------------------ | :------------------------------------- |
| **最优替换算法**        | OPT / MIN     | 淘汰未来最久才会被访问的页                                                      | 理论最优，实际无法实现                 |
| **先进先出**            | FIFO          | 按页进入物理内存的顺序，淘汰最早进入的页                                        | 实现简单，存在 Belady 异常             |
| **二次机会算法**        | Second Chance | 给队列中的页加标记，换页时若队头有标记则清除并移至队尾，否则换出队头            | FIFO 改进版，近似 LRU                  |
| **最近最少使用**        | LRU           | 淘汰过去最久没有被访问的页                                                      | 性能接近 OPT，实现开销大               |
| **最近最常使用**        | MRU           | 淘汰最近被访问的页                                                              | LRU 的反向策略，适用于特定访问模式     |
| **时钟算法/最近未使用** | Clock / NRU   | 用访问位和修改位近似模拟 LRU，通过循环链表指针扫描，优先选择访问位为 0 的页换出 | 性能接近 LRU，实现简单，Linux 广泛采用 |
| **最常使用**            | MFU           | 淘汰访问次数最多的页                                                            | 认为访问多的页未来不会再频繁访问       |
| **最不常使用**          | LFU           | 淘汰访问次数最少的页                                                            | 对冷启动页不公平，难以适应访问模式变化 |


### 题型3：颠簸（Thrashing）现象
- **考查方式**：为什么频繁缺页会导致CPU利用率下降？
- **答案**：频繁缺页导致进程等待磁盘I/O，CPU空闲；调度器误以为CPU空闲而载入更多进程，加剧内存压力，形成恶性循环。

---

## 第6章：进程与线程
### 题型1：fork() 进程复制与输出次数（新题）
- **考查方式（PDF原题）**：
  ```c
  void main() {
      int a = 0;
      int rc = fork();
      a++;
      if (rc == 0) {
          rc = fork();
          a++;
      } else {
          a++;
      }
      printf("Hello %d\n", a);
  }
  ```
  问Hello出现几次？每次a的值为多少？
- **方法**：画出进程树。
  - 主进程（P0）fork出P1。
  - P1进入if分支再fork出P2。
  - 总进程数 = 3？实际上P0、P1、P2共3个进程，但注意P0在else中`a++`，P1先`a++`再进入if`a++`，P2类似。
  - 计算各进程a值：P0（父）：初始0，fork后a=1，进入else a=2，打印2。P1（子）：初始0，fork后a=1，进入if，fork后a=2，打印2。P2（孙）：初始0（继承P1的a=0？注意fork复制时a=0），fork后a=1，进入if（rc==0）a++ => 2，打印2。共3次，a均为2。
- **答案**：Hello出现 **3次**（注意是3个进程），每次 **a=2**。（旧版答案4次是陷阱，需看具体代码逻辑）。

### 题型2：线程并发竞争条件（balance++）
- **考查方式（PDF填空/选择）**：三个线程各循环200次（或20000次）累加 `balance`，问最终结果是否确定？
- **方法**：
  - 若 `pthread_join` 串行执行（上一个join完再创建下一个）：结果为确定值（200/600等）。
  - 若三个线程并发执行（同时创建再join）：`balance++` 非原子，结果不确定，通常小于理论值。
- **答案**：串行时确定；并发时不确定（存在数据竞争）。

### 题型3：vfork 与 fork 的区别
- **考查方式**：`vfork` 为什么比 `fork` 快？有什么限制？
- **答案**：`vfork` 不复制页表，直接共享父进程地址空间，但**必须**在子进程中立即调用 `exec` 或 `_exit`，否则会破坏父进程栈。

---

## 第7章：调度（计算大题绝对核心）
### 题型1：经典调度算法甘特图（FCFS, SJF, RR, SRTF）
- **补充细节（PDF）**：最短完成时间优先（STCF）是抢占式SJF，每次新任务到达或时钟中断时比较**剩余时间**。
- **方法**：务必区分“非抢占式SJF”和“抢占式SRTF/STCF”。画甘特图时，抢占式需在到达时刻打断当前进程。

### 题型2：MLFQ（多级反馈队列）规则
- **考查方式**：描述MLFQ如何避免饥饿？如何适应短任务？
- **答案**：规则：新任务进最高优先级；同队列轮转；用完时间片降级；饥饿时**定期将所有任务提升至最高优先级**。

### 题型3：步幅调度（Stride Scheduling）计算
- **补充（PDF）**：给定 `Ticket`（如 A=30, B=50, C=60），求 `Stride`，并追踪前几步调度。
- **方法**：`MaxStride = lcm(30,50,60)=300`。`Stride_A=10, Stride_B=6, Stride_C=5`。每次选Pass最小者，Pass+=Stride。调度顺序：C(5), B(6), C(10), A(10)...

### 题型4：实时调度（RM vs EDF）
- **补充（PDF）**：问 `U=5/13 + 3/5 = 64/65 < 1`，RM是否一定可调度？
- **答案**：不一定。RM有额外调度条件（$U \le n(2^{1/n}-1)$，n=2时约82.8%），64/65≈98.4% > 82.8%，RM不可调度，但EDF可调度（因为U≤1）。

---

## 第8章：进程间通信（IPC）
### 题型1：共享内存 vs 消息传递
- **考查方式**：共享内存为什么最快？主要挑战是什么？
- **答案**：共享内存直接通过页表映射物理内存，**零拷贝**，无需陷入内核（除建立映射外）。主要挑战是**同步**（防止覆盖或读取空数据）。

### 题型2：管道（Pipe）特点
- **考查方式**：管道（Pipe）和消息队列（Message Queue）的区别。
- **方法**：管道是**字节流**、**单向**、**固定大小缓冲区**、**最多两个进程**；消息队列是**带类型消息**、**多对多**、**链表动态分配**。

---

## 第9章：同步原语（死锁与银行家算法）
### 题型1：死锁四个必要条件与预防（新题）
- **考查方式**：简述死锁的四个必要条件，并针对“持有并等待”给出预防方案。
- **答案**：互斥、持有并等待、非抢占、循环等待。预防持有并等待：**一次性申请所有资源**（使用`trylock`，失败则全部释放）。

### 题型2：银行家算法安全性检查（大题）
- **方法（已覆盖）**：计算Need -> 找Need<=Available的进程 -> 假分配释放 -> 重复。若存在安全序列则安全。

### 题型3：条件变量（Cond）与互斥锁配合
- **考查方式**：为什么 `cond_wait` 必须放在 `while` 循环中，而不是 `if`？
- **答案**：防止**虚假唤醒**（线程被意外唤醒但条件并未满足）。

### 题型4：读写锁的偏向性
- **考查方式**：偏向读者的读写锁有什么问题？偏向写者如何解决？
- **答案**：偏向读者可能导致**写者饥饿**；偏向写者保证写者等待时新读者阻塞，更加公平。

---

## 第10章：多核与多处理器
### 题型1：虚假共享（False Sharing）判断（新题）
- **考查方式（PDF原题）**：
  ```c
  struct { int a; int b; char c[64]; } data;
  // vs
  struct { int a; char c[64]; int b; } data;
  ```
  两个线程分别写 `a` 和 `b`，哪种结构更快？
- **方法**：缓存行通常64字节。第一种 `a` 和 `b` 可能在同一缓存行，导致缓存行乒乓（写失效），性能差。第二种用 `char c[64]` 隔开，`a` 和 `b` 在不同缓存行，无虚假共享，性能好。
- **答案**：**右边快**。

### 题型2：MCS锁原理
- **考查方式**：MCS锁如何解决普通自旋锁的可扩展性断崖？
- **答案**：每个等待线程自旋在自己的**本地节点**（独立缓存行），而非全局锁变量，避免单一缓存行的高频失效风暴。

### 题型3：阿姆达尔定律（Amdahl's Law）
- **补充**：若串行比例占20%，无限核心时最大加速比 = 5倍。

---

## 第11章：文件系统
### 题型1：目录项占用空间比较（新题）
- **考查方式（PDF）**：10个电影文件（movie1~10.mp4）的目录和100个笔记文件（note1~100.md）的目录，哪个占空间大？
- **答案**：目录大小取决于**目录项数量**（文件名+inode映射），而非文件内容。100个笔记文件的目录项更多，占用空间更大。

### 题型2：inode最大文件大小（已覆盖）
- **补充**：指针8B，块4KB，12直接+3间接+1二级间接，最大约1GB。

### 题型3：FAT为什么不支持硬链接（主观题）
- **考查方式（PDF原题）**：FAT不支持硬链接，困难在哪？如何修改？
- **答案**：FAT用簇链表组织文件，目录项直接存起始簇号，无独立inode节点。若多个目录项指向同一簇链，删除时无法判断是否释放数据。修改：引入**inode表**和**引用计数**。

---

## 第12章：文件系统崩溃一致性
### 题型1：JBD2三种日志模式对比
- **考查方式**：哪种模式最快？哪种最安全？
- **答案**：
  - **Writeback**（写回）：最快，只记元数据，数据无保护。
  - **Ordered**（顺序）：默认，元数据记日志，数据先落盘。
  - **Journal**（完整）：元数据+数据都记日志，最安全，写两次。

### 题型2：日志（Journal）与写时复制（CoW）优缺点
- **考查方式**：简述两者优缺点。
- **答案**：日志有写放大（双写），但恢复快；CoW无日志，天然原子性，支持快照，但递归复制写放大更严重（尤其随机写）。

---

## 第13章：设备管理
### 题型1：DMA操作顺序（新题）
- **考查方式（PDF原题）**：使用DMA从硬盘读数据到内存，顺序为：①DMA发中断；②初始化DMA并启动硬盘；③传输数据；④执行ISR。正确的顺序是？
- **答案**：**②→③→①→④**（选B）。

### 题型2：中断上下半部（Top Half / Bottom Half）
- **考查方式**：硬中断（ISR）为什么必须短？软中断（Softirq）、Tasklet、工作队列的区别？
- **答案**：硬中断屏蔽中断，必须快速处理；软中断（静态）、Tasklet（动态，同类型串行）、工作队列（进程上下文，可睡眠）。

---

## 第14章：系统虚拟化
### 题型1：Type-1 vs Type-2 Hypervisor
- **考查方式**：VMware ESXi 属于哪类？VirtualBox 呢？
- **答案**：ESXi 是 Type-1（裸金属）；VirtualBox 是 Type-2（宿主型）。

### 题型2：硬件虚拟化（VT-x / ARM EL2）
- **考查方式**：Intel VT-x 的 VMCS（虚拟机控制结构）作用？
- **答案**：VMCS 保存 VM Entry/Exit 时的 CPU 状态（寄存器、PC、PSTATE），由硬件自动保存/恢复。

---

## 第15章：操作系统安全
### 题型1：BLP模型（Bell-LaPadula）规则
- **考查方式**：简单安全属性（Simple Security Property）和星属性（Star Property）分别是什么？
- **答案**：**不可读上**（主体安全级别不能读更高级别对象）；**不可写下**（主体不能写入更低级别对象），即“下读上写”（No Read Up, No Write Down）。

### 题型2：Meltdown攻击原理
- **考查方式**：Meltdown如何利用预测执行攻击内核内存？
- **答案**：CPU预测执行时**不检查权限**，攻击者触发预测执行非法读取内核数据，根据数据值改变缓存状态，再通过**侧信道（缓存信道）** 反推数据。防御靠**页表隔离（KPTI）**。

---

## 第16章：网络协议栈
### 题型1：sk_buff指针操作（skb_push / skb_pull）
- **考查方式**：发包时添加TCP头用 `skb_push` 还是 `skb_put`？
- **答案**：**`skb_push`**（在头部增加空间，data指针上移）。

### 题型2：收包流程排序（已覆盖）
- **补充**：硬中断中分配 `sk_buff`，软中断 `NET_RX` 中调用 `ip_rcv`（网络层）和 `tcp_v4_rcv`（传输层）。

---

**最后应试提醒（针对PDF中的互动题）**：
- 遇到伙伴系统判断，**先比较地址差，再看是否等于块大小**。
- 遇到 fork 输出题，**画进程树，逐进程计算变量值**，不要凭直觉。
- 遇到调度甘特图，**务必留出足够草稿空间，逐时间推进**。

祝你考试顺利！