# 第8章 进程间通信 精简提纲
[TOC]
## 8.1 通信基础
### 8.1.1 进程的基本概念
#### 进程组成
1. 静态部分：程序代码、数据（ELF载入）
2. 动态部分：PC、堆、栈、运行状态
#### 内存特征
每个进程拥有独立虚拟地址空间，包含独立内核栈、内核代码数据
### 8.1.2 独立进程的核心问题
1. 功能模块重复开发，冗余度高
2. 自研模块性能差，无专业优化
3. 数据无法互通，重复计算浪费资源
### 8.1.3 协作进程的优势
1. 模块化拆分，功能可复用
2. 多进程并行，提升计算速度
3. 数据共享，消除重复运算
### 8.1.4 IPC定义
1. 概念：进程借助内核/共享资源传递数据、控制信号，实现进程协作
2. 通信角色：发送/接收、客户端/服务端、调用/被调用
3. 完整流程：用户态IPC调用 → 内核处理 → 结果返回用户态
## 8.2 共享内存
### 8.2.1 共享内存原理
#### 定义
内核分配一块物理内存，映射至多个进程虚拟地址，**速度最快IPC**
#### 实现方式
修改各进程页表，虚拟地址指向同一片物理内存
#### 核心难题
无原生同步机制，需自行处理：
1. 发送方不能覆盖未读数据
2. 接收方不能读取空缓冲区
### 8.2.2 共享内存基础环形缓冲区实现
1. 共享结构体定义
```c
#define BUFFER_SIZE 10
typedef struct {
    item buffer[BUFFER_SIZE];
    int in;  // 写入下标
    int out; // 读取下标
}
```
2. 生产者发送逻辑（无空间则忙等）
```c
while(new_package) {
    while (((in + 1) % BUFFER_SIZE) == out); // 缓冲区满阻塞
    buffer[in] = item;
    in = (in + 1) % BUFFER_SIZE;
}
```
3. 消费者接收逻辑（无空间则忙等）
```c
while (wait_package) {
    while (in == out); // 无数据阻塞
    item = buffer[out];
    out = (out + 1) % BUFFER_SIZE;
    return item;
}
```
### 8.2.3 共享内存固有缺陷
1. 缺少原生同步互斥机制
2. 空/满状态循环轮询，CPU空耗
3. 单纯忙等时延不可控、整体效率低
## 8.3 消息传递（微内核）
### 8.3.1 消息传递基础
1. 底层载体：可搭配共享内存存储真实数据
2. 基础原语：Send(发送)、Recv(接收)
3. 通信流程：建立连接 → Send/Recv收发消息
### 8.3.2 直接通信
#### 规则
进程使用唯一进程标识点对点通信
#### 接口
Send(P, msg)、Recv(Q, msg)
#### 特性
1. 自动建立一对一连接
2. Recv默认阻塞，直至对应进程发送消息
#### 代码实现
- 生产者：环形缓冲区写入 + Send发送通知
- 消费者：Recv阻塞等待通知，再读取buffer数据
### 8.3.3 间接通信（信箱Mailbox）
#### 核心规则
消息存入公共信箱中转，不直接指定目标进程
#### 操作接口
创建信箱、Send(M, msg)、Recv(M, msg)、销毁信箱
#### 特性
1. 多对多通信，多个进程共用同一个信箱
2. 信箱仅做同步唤醒，真实数据存共享环形缓冲区
#### 多接收端问题与方案
1. 问题：多进程同信箱接收，消息归属不可控
2. 解决策略（任选一种）
    - 信箱仅绑定两个进程
    - 同一时刻仅允许一个进程执行Recv
    - 内核随机分配接收进程
#### 代码示例
生产者写入buffer，向公共信箱发送通知；多个消费者阻塞Recv信箱消息后读取数据
### 8.3.4 同步/异步消息传递
#### 阻塞（同步）
Send/Recv阻塞等待操作完成
- 优点：时延低、编程简单
#### 非阻塞（异步）
调用后直接返回，不等待消息送达/抵达
- 优点：吞吐带宽更高
### 8.3.5 超时机制
#### 作用
避免永久阻塞，防范服务拒绝攻击
#### 接口格式
Send/Recv携带超时参数
#### 三种等待模式
永久阻塞、完全非阻塞、限时等待
#### 代码逻辑
发送超时执行重试，接收超时直接返回错误码
### 8.3.6 通信缓冲分类
1. 零容量：无缓冲，发送方必须阻塞等待接收方
2. 有限容量：固定缓冲区，满时发送阻塞
3. 无限容量：资源充足下无上限，发送几乎无阻塞
## 8.4 管道(宏内核)
### 8.4.1 Go协程管道Channel
1. 用途：协程间通信通道
2. 基础收发代码完整保留
### 8.4.2 Unix管道
#### 定义
传统Unix IPC，属于间接消息传递
#### 典型场景
shell管道命令 `ls | grep xxx`
#### 核心特性
1. 单向字节流通信
2. 依托文件描述符实现
3. 仅支持两个进程通信
4. 缓冲区满/空时自动阻塞
#### 结构
`int fd[2]`：fd[0]读端，fd[1]写端
### 8.4.3 Xv6管道底层实现
#### 管道结构体
```c
struct pipe {
    struct spinlock lock;
    char data[PIPESIZE];
    uint nread;
    uint nwrite;
    int readopen;
    int writeopen;
};
```
#### 管道写流程
加锁 → 判断缓冲区满则休眠等待读进程 → 循环写入字节 → 唤醒读端 → 释放锁
完整pipewrite代码保留
#### 管道读流程
加锁 → 缓冲区空则休眠等待写进程 → 循环读取字节 → 唤醒写端 → 释放锁
完整piperead代码保留
### 8.4.4 管道优缺点
1. 优点：实现简单，轻量适配简单数据流
2. 缺点
    - 无消息类型标识，需自行解析字节流
    - 缓冲区固定不可扩容
    - 单向传输
    - 最多两个进程通信
## 8.5 消息队列
### 8.5.1 消息队列基础
#### 定义
带消息类型的链表式间接IPC，支持异步通信
#### 存储结构
链表存储，整体遵循FIFO
单消息结构：整型type + 业务数据
#### 系统API
ftok、msgget、msgsnd、msgrcv、msgctl
#### 核心特性
1. 多进程可同时读写同一队列
2. 支持按消息类型筛选读取
### 8.5.2 Protobuf序列化
1. 使用方式：编写proto文件，工具生成多语言序列化代码
2. 流程：发送序列化Marshal、接收反序列化Parse
3. 优势：跨语言、体积小、解析速度快、字段兼容扩展
### 8.5.3 消息队列读取规则
1. 默认读取：FIFO队首消息
2. 按类型读取
    - type=0：取队列第一条消息
    - type>0：匹配对应类型的第一条消息
### 8.5.4 消息队列 vs 管道对比表
| 对比项   | 消息队列          | 管道         |
| -------- | ----------------- | ------------ |
| 缓冲区   | 动态链表          | 固定数组     |
| 消息格式 | 带类型            | 无类型字节流 |
| 通信进程 | 多收多发          | 最多两个     |
| 读取规则 | FIFO + 按类型筛选 | 仅FIFO       |