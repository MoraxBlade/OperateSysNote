# 第6章 进程与线程 精简提纲
[TOC]
## 6.1 进程
### 6.1.1进程的概念与诞生
#### 引子：从Hello World理解进程
1. 定义：同一程序多运行实例，OS独立管理单元为进程
2. 核心问题：多并行程序抽象管理由进程实现
#### 进程的诞生：从单任务到多任务
1. 早期单任务系统
    - 串行执行，CPU、外设利用率低
    - I/O等待造成大量CPU空闲
2. 分时操作系统核心思路
    - 任务抽象为进程，保存独立执行状态
    - 上下文切换：保存/加载进程状态，复用CPU
    - 进程调度：OS决策下一个运行进程
#### 进程的定义：运行中的程序
1. 两部分组成
    - 静态：代码、数据段（ELF加载）
    - 动态：PC、堆、栈、寄存器等运行状态
2. 独立虚拟地址空间，含内核栈、内核代码数据
3. 内核保存进程执行上下文（寄存器、返回地址）
#### 进程的查看命令
- cat /proc/PID/maps：进程内存布局
- ps -aux：系统全部进程信息
### 6.1.2进程的状态
#### 进程的五种核心状态
1. 新生New：创建进程、初始化资源
2. 就绪Ready：初始化完成，等待CPU调度
3. 运行Running：占用CPU执行指令；时间片用完/高优先级抢占退回就绪
4. 阻塞Blocked：等待I/O/输入等外部事件，不在运行队列
5. 终止Terminated：执行完毕/被杀死，OS回收PCB与内存
#### 进程状态转换规则
1. 阻塞无法直接转为运行
#### Linux系统中的进程状态扩展
1. TASK_RUNNING：合并传统就绪、运行态
2. 轻/中/深度睡眠（TASK_INTERRUPTIBLE/TASK_UNINTERRUPTIBL）E：可/不可被信号唤醒睡眠（阻塞）
3. TASK_ZOMBIE：进程退出，父进程未回收PCB
### 6.1.3数据结构
#### 应用程序的原始形态：ELF可执行文件
1. 适用：Linux、安卓可执行文件/共享库/目标文件
2. 组成
    - ELF头部：文件类型、架构、入口地址
    - 程序段：映射到内存，代码/只读数据/全局数据
    - 节头表：符号表、调试信息
#### 进程控制块PCB：进程核心数据结构
1. 作用：统一管控进程全部信息
2. 存储内容
    - PID、运行状态、调度优先级
    - 虚拟内存、页表信息
    - CPU寄存器上下文
    - 打开文件、占用资源
3. Linux实现：task_struct，内置内存、线程子结构
```c
struct process_v1 {
    struct context *ctx;
    struct vmspace *vmspace;
    void *stack;
};
```
#### 进程创建流程
1. 分配PCB并初始化
2. 创建独立虚拟地址空间、分配页表
3. 初始化内核栈
4. 解析ELF，映射程序段到内存
5. 分配用户栈，载入参数、环境变量
6. 初始化运行上下文，等待调度
```c
int process_create(char *path, char *argv[], char *envp[]) {
	struct process *new_proc = alloc_process();
	init_vmspace(new_proc->vmspace);
	new_proc->vmspace->pgdir = alloc_new_page();
	init_kern_stack(new_proc->stack);
	struct file *file = load_elf_file(path);
	for loadable_seg in file.segs
	vmspace_map(new_proc->vmspace, loadable_seg);
	void *stack = alloc_stack(STACKSIZE);
	vmspace_map(cur_proc->vmspace, stack);
	prepare_env(stack, argv, envp);
	init_process_ctx(new_proc->ctx);
	... 
}
```
#### 进程上下文切换
1. 触发条件
    - 时间片耗尽、系统调用、硬件中断、高优先级抢占
2. 完整切换流程
    1. 用户态切内核态
    2. 保存当前进程所有寄存器到PCB.context
    1. 替换页表，切换目标进程内核栈
    2. 读取新进程上下文，恢复寄存器
    3. 切回用户态，新进程运行
1. 核心：上下文存储CPU寄存器，保证执行无缝恢复
### 6.1.4进程的基本操作
1. 创建：fork / spawn / vfork / clone
2. 执行：exec
3. 同步等待：wait
4. 退出：exit / abort
### 6.1.5进程创建：fork (spawn, vfork, clone)
#### 核心创建接口：fork
1. 语义：父进程复制生成几乎完全相同的子进程
2. 返回值
    - 父进程：返回子PID(>0)
    - 子进程：返回0
    - 失败：-1
3. **关键特性**
    - 父子PID不同，可并行执行
    - COW写时复制：初始共享内存，修改时复制副本
    - 共享文件描述符、文件偏移量
    - 子进程从fork下一行代码执行，不从头运行
#### fork的优缺点
1. **优点**
    - 接口无参数，简洁
    - 创建与exec解耦，灵活
    - 天然构建进程树、进程组
2. **缺点**
    - 内存拷贝开销大，COW仍存在页表复制成本
    - 大内存进程性能差
    - 多线程场景兼容性差
#### fork的替代接口
1. **vfork**
    - 特点：父子共享地址空间，不复制页表
    - 限制：子进程必须立刻exec/exit；父进程阻塞至子进程执行完成
    - 示例代码省略copy_vmspace，直接复用父进程页表
2. **posix_spawn**
    - 特点：fork+exec组合接口，一次性创建并执行程序
    - 优点：扩展性、性能好；缺点：无法在fork与exec间执行自定义逻辑
    - 函数原型、示例、底层实现逻辑完整保留
3. **clone**
    - 特点：fork增强版，flag选择性复制资源；Linux线程底层实现
    - 优点：高度可控；缺点：接口复杂，易出错
    - 实现逻辑：根据CLONE_VM判断是否共享页表，CLONE_VFORK阻塞父进程
#### Windows进程创建：CreateProcess
1. 特点：全新创建，不复制父进程；需传入程序路径、参数、安全属性
#### 进程树与进程组
1. 进程树
    - fork生成父子关系，树形结构
    - 查看命令pstree
    - PID=1 systemd为根进程；单进程唯一父进程，父可多子进程
2. 进程组
    - 组长PID=PGID，子进程默认继承父进程组
    - 可向组内所有进程发送信号，多用于shell管道
#### 现代视角：fork的争议和spork模拟
1. 缺陷：老旧设计，不适合多线程、大内存场景
2. 趋势：posix_spawn、clone逐步替代fork
### 6.1.6进程执行：exec
#### 核心接口 execve
```c
#include <unistd.h>
int execve(const char *pathname, char *const argv[], char *const envp[]);
```
1. 参数：pathname程序路径、argv命令参数、envp环境变量
#### exec 核心特性
1. 典型搭配：fork创建子进程后调用
2. 运行特点
    - 替换当前进程地址空间，原有代码/数据/堆/栈全部覆盖
    - 成功无返回；失败返回-1
    - PID不变，仅替换程序内容
#### exec 与 fork 搭配逻辑
1. 流程：fork生成子进程 → 子进程execve加载新程序
2. 意义：创建与执行分离，父子可独立处理逻辑
#### 使用注意要点
1. 成功后后续代码不执行，错误处理仅失败触发
2. argv、envp数组末尾必须以NULL收尾
### 6.1.7进程间同步：wait
#### 核心作用
1. 父进程回收子进程，避免僵尸进程
2. 获取子进程退出状态，实现进程执行顺序同步
#### 函数原型与参数
```c
#include <sys/types.h>
#include <sys/wait.h>
pid_t waitpid(pid_t pid, int *wstatus, int options);
```
1. pid：指定等待子PID，-1等待任意子进程
2. wstatus：存储退出状态
3. options：0阻塞等待，WNOHANG非阻塞
4. 返回值：成功返回子PID，失败-1
#### 完整示例代码保留，执行逻辑
1. fork创建子进程
2. 父进程waitpid阻塞等待子进程退出
3. WIFEXITED判断正常退出
#### 退出状态检查宏
- WIFEXITED：正常退出判断
- WEXITSTATUS：获取正常退出码
- WIFSIGNALED：判断子进程是否被信号终止
- WTERMSIG：获取终止信号编号
## 6.2 线程
### 6.2.1线程的概念
#### 核心定义
1. 线程：进程内部执行单元，CPU调度最小单位，轻量级进程LWP
2. 依附进程存在，单进程至少1个线程
3. 资源划分
    - 共享：进程全部静态资源（代码、全局数据、文件）
    - 私有：寄存器、PC、栈、TCB
4. 多核可并行调度，切换粒度为线程
#### 引入线程的原因
1. 进程创建/切换开销巨大
2. 并发粒度粗，内部无法并行
3. 多进程并发时资源重复分配，冗余浪费
4. 进程间IPC通信成本高
#### 线程与进程的核心对比
| 对比维度 | 线程                                         | 进程                                   |
| -------- | -------------------------------------------- | -------------------------------------- |
| 核心定位 | 操作系统**CPU调度的最小单位**                | 操作系统**资源分配的最小单位**         |
| 资源归属 | 共享进程地址空间与资源，仅私有栈、寄存器、PC | 拥有独立地址空间、文件、信号等全套资源 |
| 通信方式 | 直接读写共享数据，通信成本低                 | 需IPC机制（管道、消息队列等）          |
| 稳定性   | 一个线程崩溃可能导致整个进程崩溃             | 进程间相互隔离，互不影响               |

执行开销、并发力度等

#### 线程的基本组成
1. TID线程ID
2. PC程序计数器
3. 通用寄存器组
4. 私有用户栈、内核栈
5. TCB线程控制块（状态、优先级、所属进程）
#### 线程的分类
1. 用户态线程ULT（纤程）
    - 用户库管理，内核不可见
    - 单线程阻塞→整个进程阻塞
2. 内核态线程KLT
    - 内核直接调度管理，内核存储线程信息
#### 线程的特性
1. 共享性：同进程共享代码、文件、信号
2. 独立性：独立执行流，可单独阻塞
3. 轻量级：无需独立地址空间，切换缓存开销小
4. 依附性：进程销毁则所有线程终止
5. 并发性：单核并发、多核并行
#### 引入线程的核心优势
1. 并发创建/切换开销远低于进程
2. 共享资源，减少内存冗余占用
3. 进程内细粒度并发，适配IO密集型
4. 多核并行，提升程序吞吐
### 6.2.2线程模型
#### 作用：描述用户态线程与内核线程映射关系，决定调度、性能、多核支持
#### 多对一模型 Many-to-One N:1
1. 映射：N用户线程绑定1内核线程
2. 优点：用户态切换，无系统调用，开销极低
3. 缺点：无法多核并行；单线程系统调用阻塞全部线程
4. 应用：早期用户态线程库
#### 一对一模型 One-to-One 1:1
1. 映射：每条用户线程对应独立内核线程
2. 优点：多核并行；单线程阻塞不影响其他线程
3. 缺点：内核线程创建、管理开销偏高
4. 应用：Windows、Linux NPTL、macOS默认
#### 多对多模型 Many-to-Many N:M
1. 映射：N用户线程映射M内核线程，用户库+内核协同调度
2. 优点：兼顾前两者优势，动态调整内核线程数量
3. 缺点：实现复杂，调度策略易冲突
4. 应用：Solaris9前、Go协程调度器
### 6.2.3相关数据结构
#### 线程控制块TCB
1. 作用：存储线程状态、上下文，调度切换基础
2. 一对一线程分为内核TCB、应用TCB
    - Linux：进程/线程统一task_struct，靠资源共享区分
    - 内核TCB：寄存器、栈指针、PC、优先级
    - 用户TCB：pthread结构体 / Windows TIB
#### 线程本地存储TLS
1. 定义：线程私有存储，解决多线程全局变量竞争
2. 问题场景：多线程errno覆盖冲突
3. 实现
    - 每个线程独立变量副本
    - TCB索引快速定位TLS区域
    - X86 fs段寄存器、AArch64 tpidr_el0专用寻址
### 6.2.4基本操作
#### 线程创建：pthread_create
1. 功能：初始化内核线程、内核栈、用户TCB、用户栈、TLS
```c
int pthread_create(pthread_t *thread, const pthread_attr_t *attr, 
                   void *(*start_routine) (void *), void *arg);
```
1. 参数说明：thread输出TID、attr线程属性、start_routine入口函数、arg入参
2. 调用示例保留
#### 线程合并：pthread_join
1. 作用：阻塞等待目标线程结束，获取返回值，回收线程资源
```c
int pthread_join(pthread_t thread, void **retval);
```
1. 参数：thread目标线程ID、retval接收返回值
2. 完整示例代码保留
#### 线程退出：pthread_exit
```c
void pthread_exit(void *retval);
```
1. 功能：主动终止线程，返回值由pthread_join接收
2. 区别：主线程pthread_exit不杀死进程；exit/return直接销毁全部线程
#### 线程暂停：pthread_yield
```c
int pthread_yield(void);
```
1. 功能：主动让出CPU时间片，避免CPU密集线程饥饿
2. 代码示例保留
#### 线程安全代码示例
1. 串行线程：无并发竞争，结果固定600
2. 并发线程：balance++非原子操作，数据竞争，结果小于60000
两段代码完整保留
### 6.2.5上下文切换（以ChCore为例）
#### 线程上下文定义
存储全部恢复寄存器：通用x0~x30、PC(elr_el1)、用户栈sp_el0、状态spsr_el1
#### ChCore TCB与内核栈结构
1. TCB：线程信息 + 线程上下文
2. TCB下方为专属内核栈，sp_el1指向栈顶
#### 线程上下文切换完整流程
- **第一步：进入内核，保存上下文**
    1. 硬件自动切换特权级、栈，保存PC、状态寄存器
    2. 汇编手动保存全部通用寄存器，汇编代码保留
- **第二步：切换页表与内核栈**
    1. 修改页表基地址寄存器
    2. sp_el1切换至目标线程内核栈（切换分界点）
- **第三步：恢复上下文，返回用户态**
    1. 汇编读取栈中寄存器恢复，汇编代码保留
    2. eret硬件切换回用户态，加载PC、栈、CPU状态
#### 切换总结
1. 两次特权切换：用户→内核、内核→用户
2. 三次栈切换：用户栈→当前内核栈→目标内核栈→目标用户栈
3. 切换分界点：内核栈替换