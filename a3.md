# 第3章 硬件环境与软件抽象（提纲）

## 3.1 应用程序的硬件运行环境

### 3.1.1 程序的运行：用指令序列控制处理器（ISA）
#### 指令的区分
- 机器指令：二进制编码，功能简单，组合实现复杂语义
- 汇编指令：文本格式，对应机器指令
#### ISA（指令集架构）：
  - **定义**：硬件与软件之间的抽象层/接口
  - **作用**：规定指令格式、寻址方式、寄存器结构
#### ARM 架构（RISC）：
  - 指令特征：等长（32位）、单周期执行、Load/Store 结构
  - 优势：低功耗、小面积
  - 状态与指令集：
    | 状态        | 指令集       | 特点                 |
    | ----------- | ------------ | -------------------- |
    | ARM 状态    | ARM 指令集   | 32位定长，性能最强   |
    | Thumb 状态  | Thumb 指令集 | 16位定长，代码密度高 |
    | A64 (ARMv8) | AArch64      | 64位，大内存寻址     |
#### 硬件抽象意义
- 向上解耦（兼容旧软件）
- 向下规范（硬件设计目标）

### 3.1.2 冯诺依曼架构
#### 组成
- 输入/输出
- CPU（处理单元+控制单元）（计算）
- 存储器（内存）
#### 缺陷
指令与数据不区分，可能发生错乱

### 3.1.3 程序是怎样运行的
- 操作系统也是程序（含特权指令）
- ISA 分类：用户ISA（如 `mov`）、系统ISA（如 `msr_el1`）
#### 程序运行步骤：
  1. 机器码从硬盘复制到内存
  2. PC（程序计数器）记录下一条指令地址
     - 顺序执行
     - 跳转执行（控制流跳转）

### 3.1.4 寄存器、内存、运算与访存
#### 寄存器
处理器内部，访问快、容量小
  - 通用寄存器：31个64位（X0~X30）
    - 多模态：Wn 对应 Xn 低32位
      - 与虚拟地址空置无关
    - Vn（128位）：浮点/SIMD/加密（Sn:32位，Dn:64位）
  - 特殊寄存器

#### 数据处理指令（汇编）
  - 移位指令
    - `asr`：算术右移，符号位填充
    - `lsl`：逻辑左移，补0
    - `lsr`：逻辑右移，补0
    - `ror`：循环右移
  - 数据搬移：`mov`（复制）
    - 把右复制到左

#### 访存指令
  - 访存指令：加载&存储指令
  - `LDR`（加载）、`STR`（存储）
  - 寄存器类型决定存取数据长度

#### 寻址方式（AArch64）
  - 基址寻址：`[Xn]`
  - 偏移寻址：`[Xn, #偏移量]`
    - 前变址：`[Xn, #偏移量]!`（先更新再寻址）
    - 后变址：`[Xn], #偏移量`（先寻址再更新）：寻Xn，存入值是Xn+偏移
    - Xn是地址，存入是存入Xn地址

### 3.1.5 条件结构：分支与条件码
- 分支：跳转到目标代码位置
- 条件码（PSTATE 中的 NZCV）：N（负）、Z（零）、C（进位）、V（溢出）
- 分支指令：
  - 无条件跳转
  - 条件分支 `Bxx`（依赖 PSTATE）
  - 寄存器值判断：`cbz`（值为0跳转）
- 标签（.L3, .L1 等）：汇编中标记地址，翻译时转为真实地址

### 3.1.6 函数调用、返回与栈
- AArch64 调用/返回：`bl`（返回地址存 X30/LR），`ret`（从 LR 返回）
#### 帧栈机制
防止 LR 被覆盖，编译器生成保存/恢复代码
#### 运行时栈：
  - 按调用顺序排列，栈规则LIFO
  - AArch64 栈由高向低地址增长
  - SP（X31）：栈指针，指向栈顶，表示内存释放/分配
    - 向低地址移动表示分配栈空间
    - 地址＞SP表示已分配
- **栈帧**：每个函数拥有的连续内存空间
  - FP（X29）：帧指针，用于栈追踪，向前追溯调用链中的函数栈帧
  - 可禁用 FP 以提高性能（SP 与 FP 重合）

### 3.1.7 函数调用准则
#### 寄存器保存规则：
  - 调用者保存：X9~X15
  - 被调用者保存：X19~X28
#### 数据传递：
  - 参数传递：X0~X7（前8个），更多放栈
  - 返回值：X0
#### 完整寄存器分组与用途
- 含 X8 为间接结果位置
- X16/X17 为过程内部临时寄存器（IP0/1）
#### 完整帧栈结构
![\<img alt="" data-attachment-key="PMG52NXP" width="318" height="423" src="attachments/PMG52NXP.png" ztype="zimage"> | 318](attachments/PMG52NXP.png)

### 3.1.9 冯诺依曼架构的统一内存映射
- 问题：程序与数据同空间，需权限管理、适应不同内存、管理外设
- 内存权限规则（读/写/执行等）
- 特权/普通
#### 统一内存映射：
  - 虚拟地址空间：低 2^48 字节给应用
  - 操作系统、外设地址为 0xFFFF 开头（高地址）



## 3.2 操作系统的硬件运行环境

**定义**：基本功能 + OS权限执行的功能
**实现**：CPU提供不同特权级区分OS与应用程序运行环境

### 3.2.1 特权级模型、系统ISA

#### 特权级别
- 用户态 + 内核态
- 作用：区分应用程序和OS的运行权限
- 对应ISA：
  - 系统ISA（内核态）
  - 用户ISA（用户态）

#### ARM特权级别和系统ISA（AArch64）
- 特权级状态保存：PSTATE寄存器
- 异常级别（EL）：
  - EL0：用户态，应用程序
  - EL1：内核态，OS
  - EL2：虚拟机监控器
  - EL3：普通世界和安全世界通信（TrustZone）
- 寄存器：
  - 不同特权级提供不同硬件副本（SP→SP_EL0/SP_EL1）
  - 高特权级可读写低特权级寄存器
  - 特权指令：mrs（读）、msr（写）
    - 记忆：move to x from x
- 特殊/系统寄存器：
  - 1个PC
  - 4个栈寄存器（SP_EL0-3）
  - 3个异常链接寄存器（ELR_EL1-3）
  - 3个程序状态寄存器（SPSR_EL1-3）
- EL0→EL1切换场景：
  - 系统调用（svc指令）
  - 指令触发异常
  - 外设中断

### 3.2.2 异常与中断

#### 轮询
程序/设备周期性主动查询状态或资源

#### 异常机制（AArch64）
- 定义：用户态和内核态切换的机制
- 陷入/下陷：用户态→内核态
- 异常处理程序：内核处理异常的代码
- 异常控制流：不受应用程序控制

#### 触发CPU异常的事件
- 中断：外部事件，强行打断程序
- 异常：程序自身无法处理的问题
- 主动触发：系统调用（svc）

#### 中断与异常、异步与同步
- **中断（Interrupt）** ：外部硬件信号，异步，与当前指令无关
- **异常（Exception）** ：软件执行产生，同步，与当前指令相关（含系统调用）

#### 不同体系术语对应
| 通用概念 | 产生原因 | AArch64                  | x86-64                   |
| -------- | -------- | ------------------------ | ------------------------ |
| 中断     | 硬件异步 | 异步异常：重置、中断     | 中断：可屏蔽、不可屏蔽   |
| 异常     | 软件同步 | 同步异常：终止、异常指令 | 异常：Fault、Trap、Abort |

#### AArch64中断（异步异常）
- **重置（Reset）** ：最高级别异常，初始化CPU核心，系统上电或看门狗触发
- **中断（Interrupt）** ：CPU外部信号触发，如计时器、键盘

#### AArch64异常（同步异常）
- **中止（Abort）** ：失败的指令获取或数据访问
- **异常产生指令**：
  - SVC：用户程序→OS
  - HVC：客户系统→虚拟机管理器
  - SMC：Normal World→Secure World

#### x86-64术语
- 中断（异步）：
  - 可屏蔽：键盘、网络事件
  - 不可屏蔽：关键硬件崩溃
- 异常（同步）：
  - Fault（可/不可恢复）
  - Trap（无需恢复）：断点、系统调用
  - Abort（不可恢复）

#### 中断产生与GIC中断控制器
**AArch64中断分类**：
- IRQ：普通中断，低优先级
- FIQ：快速中断，高优先级，一次只能一个
- SError：系统错误，异步中止

**GIC功能**：
- 分发：管理中断、决定优先级、路由
- CPU接口：每个CPU核对应接口

**GIC中断状态**：
- Inactive → Pending → Active → Active and pending

**中断触发**：
- 电平触发：持续电平信号，适合持续通知
- 边沿触发：信号跳变，适合事件检测，抗干扰强

#### MMIO
- 统一内存映射：外设寄存器、内核空间、用户空间统一编址
- MMIO：外设寄存器映射到物理内存特殊地址段，复用ldr/str指令

**ARMv8物理内存映射**：
- 处理器簇 + GIC + 外设 + 安全状态隔离
- 地址区间：低地址（安全/非安全内存）、中段（外设寄存器）、高地址（大容量DRAM）
- 安全属性：S（仅安全世界）、SNS（根据上下文）

### 3.2.3 异常处理与异常向量表

#### 异常向量表
- 定义：CPU规定的固定内存地址的一组异常处理入口集合（每个异常级别独立）
- 地址：VBAR_EL1寄存器
- 初始化：
  1. 编写符合规范的异常向量表
  2. VBAR_EL1←基地址
  3. 开启全局中断

#### 异常处理基本流程
- 进入：保存处理器状态、准备高特权级环境、选择handler、保证隔离
- 处理：获取异常信息（系统调用参数、错误原因）
- 返回：恢复状态、返回低特权级

#### AArch64中断和异常处理（用户态→内核态）
1. **信息保存**：
   - PC→ELR_EL1
   - 异常原因→ESR_EL1
   - PSTATE→SPSR_EL1
   - 保存与特定异常相关的信息
2. **进入EL1**：
   - SP自动换用SP_EL1
   - PSTATE特权级标志设为内核态
3. **寻找handler**：VBAR_EL1指向向量表，根据异常类型、发生特权级、处理器状态选择表项
4. **返回（eret）** ：
   - SPSR_EL1→PSTATE
   - 降至EL0，SP自动换用SP_EL0
   - ELR_EL1→PC

#### x86-64中断和异常处理
- 进入：硬件将上下文和错误码存内核栈
- 寻找handler：异常向量表存handler地址（不分级）
- iret返回：恢复上下文，内核态→用户态

#### 处理器上下文（OS在特权级切换中的任务）
- 定义：应用程序需要保存的运行状态（最小处理器状态集合）
- 位置：内存
- 寄存器：通用寄存器 + 特殊寄存器（PC、SP、PSTATE，CPU保存/多副本）+ 系统寄存器

### 3.2.4 系统调用（svc）

---

## 3.3 操作系统的基本抽象和接口

### 3.3.1 分类、抽象和虚拟化
- 进程：处理器的抽象
  - 线程：并行使用多个处理器）
- 虚拟内存：物理内存的抽象
- 文件：外设的抽象

#### 抽象vs虚拟化
- 抽象：封装底层细节，提供新简化接口
- 虚拟化：接口与已有接口完全一致，伪装成原接口

### 3.3.2 进程：对处理器的抽象

#### 分时复用
- 作用：多应用高效按需运行，避免处理器浪费
- 操作：CPU时间切分为时间片（ms级），轮流分配给程序
- 切换时机：OS处理完异常/中断后，返回用户态前
- 效果：宏观"同时"运行，程序不可见

#### 进程与进程控制块PCB
**定义**：运行中的程序
- **PCB**（进程控制块）：PID、运行状态、占用计算资源
- 效果：
  - 应用程序视角：独占处理器和内存
  - OS视角：资源分配和调度基本单位

**进程切换**：
1. 更新两个进程PCB中的运行状态
2. 保存当前进程处理器上下文→PCB
3. 读取下一进程PCB→恢复处理器寄存器
4. 更换内核栈

**进程管理与组织**：
- 操作接口：创建、执行、退出
- 父子关系：父进程创建子进程，所有进程可追溯到init进程
- 通信：IPC机制

### 3.3.3 虚拟内存：对内存的抽象

**早期多进程内存管理弊端**：
- 方案1（整体换入换出）：进程切换时整个内存数据↔磁盘
  - 优点：进程间完全隔离
  - 缺点：切换速度极慢
- 方案2（物理地址分段）：不重叠物理地址范围
  - 优点：切换无需访问磁盘
  - 缺点：无法隔离、地址空间不连续

#### 虚拟内存核心思想
- 抽象层：虚拟地址层
- 地址翻译：虚拟地址→物理地址
  - 处理器：动态翻译（机制）
  - OS：配置映射规则/页表（策略）
- 地址映射：连续虚拟地址→多块不连续物理内存；未使用区域可不映射
- 独立性：不同进程相同虚拟地址映射到不同物理内存

#### 虚拟内存优势
| 优势     | 说明                 |
| -------- | -------------------- |
| 简化编程 | 连续虚拟地址空间     |
| 内存隔离 | 独立虚拟地址空间     |
| 高效利用 | 仅映射正在使用的区域 |
| 内存扩容 | 部分数据暂存磁盘     |
| 权限控制 | 不同区域设置权限     |
| 高级功能 | 写时拷贝、内存共享   |

### 3.3.4 进程的虚拟内存布局

#### 整体布局（低→高地址）

| 区域           | 位置       | 功能                     | 特点                       |
| -------------- | ---------- | ------------------------ | -------------------------- |
| 代码段（Text） | 低地址     | 可执行程序代码           | 只读、共享                 |
| 数据段（Data） | 代码段之上 | 全局/静态变量            | 初始化后固定               |
| 用户堆（Heap） | 数据段之上 | 动态内存分配（malloc）   | 自底向上扩展               |
| 代码库         | 用户堆之上 | 共享库、文件映射         | 动态加载、只读             |
| 栈（Stack）    | 高地址     | 局部变量、函数调用上下文 | 自顶向下扩展               |
| 内核空间       | 最高地址   | OS内核代码与数据         | 仅内核可访问，所有进程共享 |

#### 关键区域
- 代码段+数据段：来自可执行文件
- 堆：用于动态内存分配，程序员手动管理（malloc/free）
- 栈：自动管理，LIFO
- 内核空间：用户态不可见，仅在系统调用时切换

**Linux查看**：`cat /proc/[pid]/maps`（内核空间地址不出现）

### 3.3.5 文件：对存储设备的抽象

- 背景：内存数据易失性 + 存储设备多样性
- 抽象目标：隐藏底层差异，提供统一访问方式

#### 文件核心定义
- 本质：有名字的字节序列（文件名独立于数据）
- 目录结构：层级树形，根目录 /；路径定位文件

#### 标准文件访问接口
| 接口  | 功能     | 说明                               |
| ----- | -------- | ---------------------------------- |
| open  | 打开文件 | 返回文件描述符（fd）；指定读写模式 |
| read  | 读取文件 | fd→缓冲区                          |
| write | 写入文件 | 缓冲区→fd（如stdout的fd=1）        |
| close | 关闭文件 | 释放fd                             |

#### mmap访问方式
- **定义**：文件直接映射到进程虚拟地址空间
  - 用户程序可像读写内存一样访问文件。
- 流程：open→mmap→直接读写内存
- 参数：PROT_READ、MAP_PRIVATE/MAP_SHARED
- **优势**：效率高，适合大文件；支持共享内存

### 3.3.6 文件：对所有设备的抽象

#### 设计哲学：一切皆文件
- Linux/UNIX将所有I/O设备抽象为文件
- 用户视角：操作统一，学习成本低
- 实现视角：复用文件系统代码，简化驱动开发

#### 设备文件实现
- 位置：/dev/目录
- 访问方式：open→fd→read/write，与普通文件一致
- TTY示例：printf输出→write到TTY设备；fgets输入→read从TTY设备

#### 特殊设备扩展
- 复杂设备需额外控制接口（音频、显卡）
- 扩展抽象：ioctl发送控制命令；Socket用于网络设备

### 3.3.7 文件抽象总结
1. 存储设备的抽象：以字节序列+名字统一访问持久化存储
2. I/O设备的抽象："一切皆文件"（终端、磁盘、串口等统一用open/read/write/close）
3. 两种访问方式：传统read/write（通用）+ mmap（高效）
4. 价值：简化应用程序开发