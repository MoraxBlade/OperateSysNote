# 第十五章 操作系统安全

## 15.1 操作系统安全的三个层次

### 层次一：基于OS的应用隔离与访问控制
- **威胁模型**：OS可信，应用可能恶意或存在bug
- **隔离手段**
  - 内存数据隔离：进程间不同虚拟地址空间
  - 文件系统隔离：访问控制机制限制应用对文件的访问

### 层次二：OS对恶意应用的隔离与防御
- **威胁模型**：OS存在bug/漏洞，OS运行过程依然可信
- **防御手段**
  - 防御常见OS漏洞
  - 沙盒机制限制应用运行

### 层次三：OS不可信时对应用的保护
- **威胁模型**：OS被完全控制，恶意应用与OS串通
- **防御手段**
  - 基于Hypervisor的保护（可信基更小）
  - 基于硬件Enclave的保护（硬件更可信）

### 三个基本概念
- **TCB（可信计算基）**：实现系统安全保护的所有机制集合（软件+硬件+固件）
- **攻击面**：组件被攻击的所有方法的集合
- **防御纵深**：设置多道防线，增加攻击难度

---

## 15.2 访问控制模型

### 引用监视器（Reference Monitor）
- 主体通过引用间接访问对象，位于主体与对象之间进行检查
- **两大职责**
  - Authentication（认证）：确定发起请求实体的身份
  - Authorization（授权）：确定实体拥有访问资源的权限

### 认证机制
- **知道什么**：密码、口令、手势密码
- **有什么**：USB-key、密码器等实物
- **是什么**：指纹、虹膜、步态等生物特征

### 授权机制：文件系统权限
- **用户组模型**：文件拥有者、文件拥有组、其他用户组
- **9 bit权限**：3种权限（读r/写w/执行x）× 3类用户
- **检查时机**
  1. `open()`时：根据用户组进行鉴权，引入fd记录本次权限
  2. 每次操作文件时：根据fd信息检查
- **fd即引用**

### SUID机制（最小特权级原则）
- **问题**：`passwd`需修改`/etc/shadow`，但用户无权访问该文件
- **解决**：在`passwd`的inode中增加SUID位，执行时临时提权为root
- **效果**：进程提权时间最小化

```
-rwsr-xr-x 1 root root 68208 /usr/bin/passwd
          ↑ SUID位
```

### RBAC（基于角色的访问控制）
- 用户与权限解耦，引入**角色**概念
- 用户→角色→权限（多对多关系）
- **优势**：角色-权限关系稳定，用户-角色关系变化频繁，便于权限批量更新

### DAC vs MAC
|        | DAC（自主访问控制） | MAC（强制访问控制） |
| ------ | ------------------- | ------------------- |
| 控制方 | 对象拥有者          | 系统强制规则        |
| 特点   | 灵活                | 不可改变            |
| 示例   | 文件系统            | 军队安全等级        |
| 优先级 | 低                  | 高（可叠加DAC）     |

### Bell-LaPadula模型（BLP）
- 状态机模型，用于政府/军队等严格安全等级场景
- **两条MAC规则**
  - **简单安全属性**：安全级别为L的主体**不能读取**安全级别 > L的对象
  - ***属性（星属性）** ：安全级别为L的主体**不能写入**安全级别 < L的对象
- **一条DAC规则**：自主安全属性，使用访问矩阵
- **信息流方向**：低→高允许，高→低禁止

---

## 15.3 SELinux

### 概述
- NSA发起，2003年并入Linux
- Flask安全架构在Linux上的实现（Linux Security Module）
- 提供访问控制框架，支持多种安全策略（含MAC）

### 核心概念
| 概念                           | 含义                                             |
| ------------------------------ | ------------------------------------------------ |
| 用户（User）                   | 系统中的用户，与Linux系统用户无关                |
| 策略（Policy）                 | 一组规则的集合（Targeted/MLS/Minimum）           |
| 安全上下文（Security Context） | 主体和对象的标签，格式：`用户:角色:类型:MLS层级` |
| 类型（Type）                   | 对象的type；进程的type称为domain                 |

### 访问向量
- **问题抽象**：`<主体>`是否能在`<对象>`上做`<操作>`
- **AVC（Access Vector Cache）**：先查AVC，未命中则查安全服务器

---

## 15.4 操作系统内部安全

### 漏洞分类
| 维度     | 内容                                                                   |
| -------- | ---------------------------------------------------------------------- |
| 漏洞类型 | 栈/堆溢出、整形溢出、空指针、UAF、格式化字符串、竞争条件、参数检查错误 |
| 攻击模块 | 调度、内存管理、通信、文件系统、设备驱动                               |
| 攻击效果 | 提权、任意代码执行、内存篡改、窃取数据、DoS、破坏硬件                  |

### 典型漏洞：整形溢出
```c
unsigned long count = /* from user space */;
if(count > 1 << 30) return -EINVAL;
table = vmalloc(sizeof(struct rps_dev_flow_table) + count * sizeof(struct rps_dev_flow));
for(i = 0; i < count; i++) { table->flow[i] = /* ... */; }
```
- count = 2³⁰ 时，count × 8 = 2³³，32位系统整形溢出 → 分配空间不足 → 越界访问

### ret2usr攻击
- 内核与用户态共享页表 → 内核可执行用户态代码
- 利用漏洞修改内核函数指针/栈返回地址 → 跳转到用户态恶意代码

### ret2dir攻击（绕过SMEP）
- **SMEP**：硬件保证CPU在内核态时不得执行用户态代码（Intel） / PXN（ARM）
- **绕过**：利用内核直接映射区域，推算恶意代码在该区域的虚拟地址，跳转执行
- **前提**：直接映射区域可执行（Linux 3.8.13及以前为RWX）
- **缓解**：KASLR（内核地址空间布局随机化）

### Rootkit
- 以获取root权限为目的的恶意软件
- 用户态Rootkit：注入root权限进程
- 内核态Rootkit：修改内核代码/数据

---

## 15.5 侧信道与隐秘信道

### 定义对比
|          | 隐秘信道（Covert Channel）             | 侧信道（Side Channel） |
| -------- | -------------------------------------- | ---------------------- |
| 通信双方 | 串通                                   | 攻击者 vs 被攻击者     |
| 目的     | 主动传输信息                           | 被动窃取信息           |
| 载体     | 非通信机制（时间、功耗、电磁、声音等） | 同左                   |

### 时间信道攻击示例
```c
checkpw(user, passwd) {
    acct = accounts[user];
    for (i = 0; i < len(acct.pw); i++) {
        if (acct.pw[i] != passwd[i]) return False;
    }
    return True;
}
```
- **攻击原理**：不同位存放在不同内存页
  - 第1位猜错 → 立即返回（快）
  - 第1位猜对 → 触发第2位所在页换入（慢）
- **效果**：8位密码（62种字符/位），暴力破解需62⁸次，时间信道仅需62×8次

### 缓存信道攻击

**四种攻击方式**
| 攻击方式     | 原理                                                   |
| ------------ | ------------------------------------------------------ |
| Flush+reload | 清空缓存 → 等待目标执行 → 访问变量，测时判断是否在缓存 |
| Flush+flush  | 基于clflush执行时间差异推测数据缓存状态                |
| Prime+probe  | 填充缓存集 → 等待目标执行 → 探测缓存集被驱逐情况       |
| Evict+time   | 驱逐缓存行 → 等待目标执行 → 测时判断是否重载           |

**防御根本方法**：不共享（物理隔离）
**ORAM（不经意随机访问内存）** ：定时、定量、定位访问，解耦访存与执行，但引入大量额外负载

---

## 15.6 Meltdown经典案例

### 预测执行机制
- CPU为性能进行预测执行，若条件不满足则抛弃结果
- **漏洞**：预测执行时**不进行权限检查**，可跨权限非法访问内核数据

### 攻击流程
1. 触发预测执行：`if (x != 0) y = a[10];`，使CPU预测执行非法内存访问
2. 非法访问内核数据，根据数据值改变缓存状态
3. 条件不满足，CPU丢弃执行结果（不报错）
4. 攻击者通过缓存信道反推出内核数据

```
属于隐秘信道（Covert Channel）：攻击者构造缓存状态 + 读取缓存状态，中间隔的是用户态与内核态的隔离
```

- **攻击能力**：最高 503KB/s 探测内核内存
- **防御**：页表隔离（内核单独页表），不与应用共享地址空间
  - 代价：TLB刷新，部分应用性能降低达30%

---

## 15.7 恶意操作系统

### 威胁模型
"除了应用，别的都不可信"（自包含的信任）

### 恶意OS攻击应用的手段
- **窃取数据**：OS控制页表，直接映射应用内存读取
- **改变执行流**：在应用内部新映射恶意代码，任意改变执行流

### 硬件隔离环境：Enclave / TEE
- **TrustZone**（2003）：硬件隔离，但层次复杂，内部软件漏洞不断
- **SGX**（2015）：以Enclave粒度保护，但交互漏洞频频

### SGX关键技术
1. **内存加密与完整性保护**：CPU外皆密文，Merkle Tree保护完整性
2. **远程验证（Remote Attestation）**：远程判断主体是否为Enclave
3. **隔离**：Enclave内部与外部隔离

### 粒度对比
| 抽象层次 | 粒度 | 特点               |
| -------- | ---- | ------------------ |
| 应用片段 | 细   | 内部简单，交互复杂 |
| 应用     | ↓    |                    |
| 容器     |      |                    |
| 虚拟机   |      |                    |
| 物理机   | 粗   | 交互简单，内部复杂 |

### 防御策略
- 基于权限控制：OS无权访问用户数据
- 基于加密：OS即使访问也无法解密
- 权限控制 + 加密：软件防御 + 硬件防御

### AMD SEV
- 以虚拟机为粒度的Enclave
- 每个VM密钥不同，Hypervisor有自己的密钥
- **缺陷**：依然部分依赖Hypervisor（如密钥设置）

### Enclave仍面临的威胁
- 接口攻击：系统调用返回错误值（如malloc返回栈地址）
- DoS：拒绝分配计算资源
- 侧信道：Spectre、L1TF

