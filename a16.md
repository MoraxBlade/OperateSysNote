
# 第十六章 网络协议栈与系统实现

## 16.1 网络协议分层模型

### 分层结构
| 层             | 功能                       |
| -------------- | -------------------------- |
| 应用层         | 用户进程（通过端口号识别） |
| 传输层         | TCP/UDP协议处理            |
| 网络层         | IP协议处理、路由           |
| 网络协议接口层 | 协议无关接口               |
| 网络设备接口层 | 设备无关接口               |
| 设备驱动层     | 网卡驱动                   |
| 物理层         | 网卡硬件                   |

### 套接字编程模型
- 端口号识别应用进程
- 多路复用技术：select、poll、epoll、kqueue

---

## 16.2 网络驱动模型

### 中断机制
- **硬中断（ISR）** ：网卡通知CPU收到数据包，查看`/proc/interrupts`
- **软中断（softirq）** ：下半部处理，查看`/proc/softirqs`（NET_RX）
- **中断合并（Interrupt Coalescing）** ：累积到阈值或超时再发中断
  - 优点：更高吞吐量
  - 缺点：中断响应延迟增加

### 收发统计
```bash
ifconfig
# RX packets / TX packets
# RX errors / dropped / overruns
```

### 三种内核架构对比
- **宏内核**：协议栈在内核态，系统调用开销
- **微内核**：协议栈在用户态服务，IPC开销
- **LibOS**：协议栈在用户态库，函数调用

---

## 16.3 Linux系统收包过程

### 函数视角
```
网卡DMA → rx_ring → 硬中断(ISR) → 分配sk_buff → 入队input_pkt_queue 
→ 软中断NET_RX_SOFTIRQ → 移skb到process_queue 
→ 网络层(ip_rcv) → 传输层(tcp_v4_rcv) → 查找socket → 唤醒用户进程
→ 用户态read → tcp_recvmsg → 拷贝数据到用户缓存
```

### 详细数据视角
1. **DMA**：网卡数据帧→内核内存`rx_ring`
2. **硬中断**：CPU调用网卡ISR → 分配`sk_buff` → 包入队`input_pkt_queue` → 触发软中断
3. **软中断**：`input_pkt_queue`→`process_queue` → 按协议类型分发
4. **网络层（`ip_rcv`）** ：检查IP包 → 路由判断（本机/转发/丢弃）
5. **传输层（`tcp_v4_rcv`）** ：检查TCP头 → 查找socket → 调用`tcp_prequeue()`将载荷送入socket接收队列
6. **应用层**：唤醒socket → `read/recvfrom`系统调用 → `tcp_recvmsg()` → 拷贝到用户缓存

### 经典面试题：数据拷贝次数

**问：从网卡到用户进程，网络包数据至少要拷贝几次？**

**答：至少2次**

| 阶段      | 拷贝类型 | 说明                                      |
| --------- | -------- | ----------------------------------------- |
| 网卡→内核 | DMA      | 一次数据拷贝                              |
| 内核→用户 | CPU拷贝  | 必须拷贝（内核/用户态隔离），一次数据拷贝 |
| skb管理   | 0次      | skb只保存指针，不拷贝数据                 |
| 协议层间  | 0次      | 复用缓存数据                              |

---

## 16.4 sk_buff结构

### 核心数据结构
```c
struct sk_buff {
    struct sk_buff *next, *prev;  // 双向链表
    unsigned int len, data_len;
    unsigned char *head;           // 缓冲区头部
    unsigned char *data;           // 当前层数据头部
    sk_buff_data_t tail;           // 当前层数据尾部
    sk_buff_data_t end;            // 缓冲区尾部
    char cb[48];                   // control buffer（每层私有信息）
};
```
- **sk_buff本身不存储报文数据**，通过指针指向真正的报文内存

### 收包过程：指针调整（逐层剥离头部）
| 阶段      | data指针变化                       |
| --------- | ---------------------------------- |
| 链路层    | data指向以太网头                   |
| →网络层前 | data向下移动（skb_pull），指向IP头 |
| →传输层前 | data继续下移，指向TCP头            |

### 发包过程：指针调整（逐层添加头部）
| 阶段             | 操作                                             |
| ---------------- | ------------------------------------------------ |
| 1. alloc_skb()   | 申请缓冲区                                       |
| 2. skb_reserve() | 预留头部空间                                     |
| 3. TCP层         | 填入应用层数据（skb_put）→ 填入TCP头（skb_push） |
| 4. IP层          | 填入IP头（skb_push）                             |
| 5. 链路层        | 填入以太网头（skb_push）                         |

### 关键操作函数
| 函数                    | 功能                                   |
| ----------------------- | -------------------------------------- |
| `skb_push(skb, len)`    | data指针上移，头部增加空间（发包添头） |
| `skb_put(skb, len)`     | tail下移，尾部增加空间（发包添数据）   |
| `skb_pull(skb, len)`    | data下移，去除头部（收包剥头）         |
| `skb_reserve(skb, len)` | 头部预留空间                           |
| `skb_trim(skb, len)`    | 缩减报文长度                           |

### skb管理函数
| 函数          | 类型   | 用途                                           |
| ------------- | ------ | ---------------------------------------------- |
| `alloc_skb()` | 分配   | GFP_ATOMIC（中断上下文）/ GFP_KERNEL（可睡眠） |
| `skb_clone()` | 浅拷贝 | 复制控制结构，共享报文（如tcpdump）            |
| `skb_copy()`  | 深拷贝 | 复制控制结构+报文（如NAT）                     |
| `kfree_skb()` | 释放   |                                                |

### 队列管理
- `sk_buff_head`：双向链表头，含qlen和自旋锁
- **NAPI批处理**：累积足量skb后一次性处理（`netif_receive_skb_list()`）

---

## 16.5 Linux系统发包过程

```
应用层write → 系统调用 → TCP层(alloc_skb → skb_reserve → skb_put数据 → skb_push头部)
→ IP层(skb_push头部) → 链路层(skb_push头部) → 驱动队列 → 网卡DMA发送
```

---

## 16.6 网络处理性能优化

### 性能挑战
| 挑战       | 具体问题                               |
| ---------- | -------------------------------------- |
| 中断处理   | 大量包→频繁硬中断，打断软中断/系统调用 |
| 上下文切换 | 线程调度开销，锁竞争                   |
| 内存拷贝   | 占处理流程57.1%（内核↔用户）           |
| 内存管理   | 4K页→TLB压力大                         |
| 局部性失效 | 跨核处理→Cache失效，NUMA跨节点访问     |

### 数据面与控制面分离
- **数据面**：负责数据传输与处理
- **控制面**：负责控制流程
- **思想**：解除控制与数据的耦合，数据面可全速运转

```
for(int i=0; i<10; i++) { a[i]=func(a[i]); }  // 控制+数据混合
↓ 展开
a[0]=func(a[0]); a[1]=func(a[1]); ... a[9]=func(a[9]);  // 纯数据，无分支
```

---

## 16.7 Intel DPDK

### 总体架构
- **Data Plane Development Kit**：绕过Linux内核协议栈（bypass kernel）
- **UIO（User I/O）** ：用户态访问MMIO和DMA ring buffers
- **PMD（Poll Mode Driver）** ：轮询替代中断

### 零拷贝实现
1. 通过`/dev/uioX` read感知中断
2. 通过mmap和外设共享内存

### 核心优化技术
| 技术               | 效果                       |
| ------------------ | -------------------------- |
| Huge Page（2M/1G） | 减少TLB Miss               |
| CPU亲和性（绑核）  | 减少调度切换               |
| NUMA本地内存       | 避免跨节点访问             |
| PMD驱动            | 去中断，减少内核开销       |
| 控制面/数据面分离  | 内核负责控制，用户负责数据 |

### DPDK核心库组件
- **核心库**：系统抽象、内存管理、无锁环、缓存池
- **流分类**：精确匹配、最长匹配、通配符匹配
- **软件加速库**：IP分片、报文重组、排序
- **QoS**：限速、调度
- **数据包分组架构**：多核流水线

### 局限
- DPDK本身仅提供2层协议，无socket接口
- 需配合用户态协议栈使用：F-stack、VPP、mTCP

---

## 16.8 无锁环（Lockless Ring Buffer）

### 基本特性
- 首尾相连数组，定长元素（4字节或整数倍）
- 支持多生产者/多消费者并发
- 不加锁，通过原子操作实现高性能

### 核心指针
| 指针        | 含义                             |
| ----------- | -------------------------------- |
| `prod_head` | 生产者队头（指向第一个空位）     |
| `prod_tail` | 生产者队尾（最后一个已生产位置） |
| `cons_head` | 消费者队头                       |
| `cons_tail` | 消费者队尾                       |

### 单生产者入队流程
1. 复制`prod_head`为私有变量
2. 检查`prod_head == cons_tail`？满则返回
3. 移动共享`prod_head`到`prod_next`
4. 向私有`prod_head`位置写入数据
5. 移动共享`prod_tail`到共享`prod_head`

### 多生产者入队流程（关键：CAS原子操作）
1. 复制`prod_head`为私有变量
2. 检查是否满
3. **CAS**：`if (共享prod_head == 私有prod_head)` → 共享`prod_head`移到`prod_next`
   - 成功 → 继续
   - 失败 → 从步骤1重试
4. 各进程向各自的私有`prod_head`位置写入数据
5. 更新`prod_tail`：`if (共享prod_tail == 私有prod_head)` → 更新；否则等待

### 多消费者出队
- 与多生产者对称，使用CAS原子操作更新`cons_head`和`cons_tail`

**参考实现**：FreeBSD `buf_ring.h`，Linux内核 `kfifo`

---

## 16.9 内存池（MemPool）

### 特性
- 由名称 + 无锁环共同标识存储空间
- **内存对齐**：避免同一内存通道的数据无法同时访问
  - 双通道需补齐，三通道天然对齐
- **私有缓存（Per-core Cache）** ：每个核独立缓存，减少跨核访问

### 内存控制器
- 提供物理内存抽象（逐字节可寻址大数组）
- 多通道内存可并行访问

---

## 16.10 其他DPDK模块
- **接收端负载均衡**（RSS）
- **定时器**：精准时间间隔执行异步函数
- **报文转发算法**：精确匹配、最长前缀匹配（LPM）、ACL、Cuckoo哈希

---

## 16.11 硬件优化方案：RDMA

### RDMA（Remote Direct Memory Access）
- 远端无感情况下直接读取远端内存数据
- **绕开内核协议栈**，CPU不参与数据传输
- **对比**：
  - 传统方式：多次内存拷贝 + 内核协议栈处理
  - RDMA方式：直接从内存到网卡，旁路内核