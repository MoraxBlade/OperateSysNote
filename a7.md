# 第7章 调度 精简提纲
[TOC]
## 7.1 调度的含义
### 7.1.1 调度的基本定义
#### 核心
- 协调各类硬件资源的任务请求
  - 调度器统一分配资源、决定任务执行顺序
  - 解决任务数远多于处理器的矛盾
#### 处理器调度三项核心决策
做什么、谁来做、做多久
- 选定下一个执行任务
- 指定运行CPU核心
- 设定任务执行时长
### 7.1.2 调度的适用场景
- CPU进程/线程调度
- I/O外设调度：磁盘、打印机
- 内存调度：内存分配、换页
- 网络调度：数据包排序转发
### 7.1.3 不同系统的调度目标
#### 分系统专用目标
| 系统类型   | 核心目标         |
| ---------- | ---------------- |
| 批处理系统 | 高吞吐量         |
| 交互式系统 | 低响应时间       |
| 网络服务器 | 高可扩展性       |
| 移动设备   | 低能耗、续航平衡 |
| 实时系统   | 严格截止时间     |
#### 通用调度目标
- 硬件资源高利用率
- 多任务公平，无饥饿
- 调度自身低开销
### 7.1.4 调度器细分目标
- 缩短周转时间
- 降低首次响应时延
- 满足实时截止时间
- 保证公平无饥饿
- 调度逻辑轻量化
- 任务扩容稳定可扩展
### 7.1.5 调度的挑战
- 任务执行时长、IO频率等信息不可预知
- 系统负载动态实时变化
- 进程存在同步、互斥、依赖复杂关系
- 多指标无法同时最优
- 多维度取舍矛盾：开销/效果、优先级/公平、性能/能耗
## 7.2 调度的机制
### 7.2.1 调度策略与调度机制区分
#### 概念区分
- 策略：做什么，上层调度目标/规则
- 机制：怎么做，底层代码实现方式
### 7.2.3 三类调度机制：长、短、中期
#### 长期调度（作业调度）
- **管控阶段**：进程从新建→就绪队列准入
- **作用**：控制就绪队列总任务量，宏观管控资源利用率
- **特点**：仅筛选新建进程，并非所有进程直接就绪
#### 短期调度（CPU调度）
- **管控状态**：就绪↔运行、运行→阻塞切换
- **作用**：直接分配CPU，选就绪任务执行
- **触发时机**
  - 进程时间片耗尽
  - 进程主动阻塞等待IO
  - 阻塞进程事件完成重回就绪
#### 中期调度（挂起调度）
- **触发条件**：物理内存不足
- **目标**：换出占用内存进程至磁盘，释放内存
- **优先挂起对象**：频繁缺页、长期闲置、高内存占用进程
- **两种挂起状态**
  - 挂起就绪：磁盘存放，内存充足可激活回普通就绪
  - 挂起阻塞：磁盘等待事件，事件完成转为挂起就绪
- **流转规则**：普通就绪/阻塞 ↔ 挂起就绪/阻塞

![\<img alt="" width="1065" height="410" data-attachment-key="WQXM8SJV" src="attachments/WQXM8SJV.png" ztype="zimage"> | 1065](attachments/WQXM8SJV.png)
### 7.2.3 全流程进程调度总览
- 批任务先入批队列，经长期调度创建进程入就绪；交互/实时任务直接就绪
- 就绪队列由短期调度选中上CPU
- 运行进程三类流向
  - 时间片用完 → 就绪队列
  - 等待IO → 阻塞队列
  - 执行完毕 → 回收终止
- 内存不足触发中期调度，就绪/阻塞进程挂起至磁盘
- 内存空闲/事件完成，挂起进程激活回归普通队列
### 7.2.4 Linux系统调度机制落地
#### CFS完全公平调度器
- 适配策略
  - SCHED_OTHER：普通分时进程
  - SCHED_BATCH：后台批量任务
  - SCHED_IDLE：极低优先级空闲任务
- 底层数据结构
  - struct rq：单CPU总运行队列，拆分cfs、rt子队列
  - struct cfs_rq：CFS任务红黑树队列
  - struct sched_entity：调度实体，关联task_struct
#### RT实时调度器
- 适配策略
  - SCHED_FIFO：实时先进先出
  - SCHED_RR：实时时间片轮转
- 底层数据结构
  - rt_rq：实时任务队列
  - 位图+多级优先级链表，独立run_list
  - sched_rt_entity实时调度实体，带run_slice时间片
#### Linux整体设计逻辑
上层区分调度策略，下层rq、红黑树、链表实现长/中/短期完整调度流程
## 7.3 单核调度策略
### 7.3.1 调度场景类比
老师=单核CPU；学生任务=进程；排队=就绪队列
### 7.3.2 经典调度算法
#### FCFS先来先服务
- 规则：按到达顺序，非抢占，进程运行至结束
- 优点：实现简单、开销小
- 缺点：长任务阻塞短任务，平均周转时间高，IO任务不友好
#### SJF最短任务优先
- 规则：预估运行时间最短优先
- 分类
  - 非抢占：当前进程结束才切换
  - 抢占（最短剩余时间）：时钟中断触发抢占
- 优点：同等条件平均周转时间最优
- 缺点：长任务饥饿；需要预知完整运行时长，现实无法精准获取
#### RR时间片轮转
- 规则：分时系统，固定时间片，用完排至队尾
- 适用：交互式桌面系统
- 特性
  - 无饥饿，兼顾响应与公平
  - 片过大退化为FCFS；过小上下文切换开销暴增
- 缺点：整体周转时间变长
### 7.3.3 优先级调度
#### 基础概念
- 规则：数值越高优先级越高；分抢占/非抢占
- **优先级分配**
  - 实时任务 > 前台交互 > 后台批处理；IO密集型调高优先级
- 实现：多级独立就绪链表，同优先级RR轮转
#### 三大问题与解决方案
- **CPU/IO资源利用率低**
  - 方案：IO密集进程分配高优先级
- **优先级反转**（低进程占资源，高进程阻塞，中进程抢占低进程）
  - 解决：优先级继承，持有资源低进程临时升级至高等待进程优先级
- **低优先级进程饥饿**
  - 解决：MLFQ多级反馈队列
      - 新任务高优先级、短时间片
      - 长时间运行任务下调优先级、加长时间片
      - 定期全任务提升优先级，杜绝永久饥饿
### 7.3.4 彩票调度（公平共享）
#### 公平共享核心思想
- CPU时间按任务权重比例分配，由票数代表权重
- 总票数T，随机数R落在任务票区间则调度该任务
#### 核心参数
- Ticket：权重票数，占比=CPU时间占比
- 调度步骤
  - 累加全部任务总票T
  - 生成随机R∈[0,T)
  - 遍历累加票数，首个sum>R任务运行
- 优化：跳表替代链表，查找由O(n)→O(logn)
#### 拓展机制：彩票转让
- 场景：RPC客户端阻塞等待服务端
- 逻辑：客户端临时转让全部ticket给服务端，处理完成返还
- 收益：提升服务端调度概率，加快请求处理
#### 优缺点
- 优点：实现简单；动态改票数即可调整权重；无绝对饥饿
- 缺点：随机带来CPU占比波动，分配不精确
### 7.3.5 步幅调度（彩票确定性版本）
#### 三大参数
- Ticket：权重票
- Stride步长：$Stride=MaxStride/Ticket$；权重越大步长越小
- Pass虚拟累计运行时间，初始0，每次调度后 $Pass+=Stride$ 
#### 调度规则
- 每次选取就绪队列Pass最小任务运行
- 运行结束更新Pass放回队列
- 长期CPU占用比例严格等于Ticket权重比
#### 步幅 vs 彩票调度对比
彩票：随机，短期波动大；步幅：纯计算，分配精准可预测
#### 工程落地：Linux CFS
- vruntime等价Pass；红黑树取最小vruntime任务
- nice值映射权重，权重越高vruntime上涨越慢
- 取消固定MaxStride，适配动态创建销毁任务
#### 优缺点
- 优点：分配精准无波动、无饥饿、调度顺序可预判、支持彩票转让
- 缺点：需持续维护Pass/Stride；原生MaxStride不适合动态任务
### 7.3.6 实时调度
#### 基础概念
- 定义：面向实时任务的处理器调度机制
- 目标：任务严格在截止时间前完成
- 任务分类
  - 硬实时：超时引发严重故障
  - 软实时：偶尔超时仅降低体验
- 实时OS特征：调度时延可预测、执行时间有确定上界
#### 利用率公式（周期任务）
单任务利用率 $U_i=C_i/T_i$
系统总利用率 $U=\sum U_i$
可调度基础条件：$U\le1$
- $C_i$：任务单次运行耗时
    
- $T_i$：任务运行周期（默认周期等价于截止时间）
#### RM速率单调调度
- 规则：静态优先级，周期越短优先级越高，抢占式
- 可调度上限：n任务约$n(\sqrt[n]{2}-1)$，双任务82.8%
- 缺陷：总U<1仍可能出现任务超时
#### EDF最早截止时间优先
- 规则：动态优先级，就绪任务中剩余截止时间最近优先抢占
- 优点：单核U≤1时100%可调度，理论最优
- 缺陷：U>1发生多米诺连锁超时
#### 实时调度硬性要求
- 调度时延确定
- 高优先级任务支持抢占
- 调度算法自身开销极低
## 7.4 多核调度
### 7.4.1 多核调度三大问题
#### 任务依赖问题
同一个进程的线程通常存在依赖关系
#### 缓存效率问题
任务在CPU核心间频繁迁移，会导致缓存命中率下降，破坏缓存局部性
#### 负载均衡问题
各核心负载不均，出现忙闲差异，影响整体吞吐与响应

### 7.4.2 面向依赖的调度策略
#### 协同调度
- 规则：无依赖任务并行；依赖任务统一同步交换数据后再执行
- 适用：机器学习、图计算、大数据平台
#### 群组调度Gang Scheduling
- 规则：关联任务划为一组，整组同时分配至不同核心运行
- 收益：避免组内线程阻塞等待
### 7.4.3 多核调度额外考量
- 多核支持线程真正并行
- 多级缓存，优先减少线程跨核迁移，保证缓存亲和
### 7.4.4 两级调度架构
#### 架构设计
- 全局调度器：任务分发、全局负载均衡
- 单CPU本地调度器：独立rq运行队列，本地选任务
#### 执行流程
- 任务进入全局调度器
- 分发至各核心本地就绪队列
- 本地调度器执行CPU分配
#### Linux实现
每个逻辑CPU独立struct rq，内置CFS、RT两套队列
### 7.4.5 负载均衡
#### 目标
将高负载核心任务迁移至空闲核心，均衡各核心压力
#### 难点
任务CPU/IO特性差异，仅队列长度无法代表真实负载
### 7.4.6 负载追踪PELT
- 粒度：单调度实体独立统计负载
- 统计周期1024μs，单周期负载 $L(t,i)=\frac{x}{1024} \times CpuScaleFactor$
- 加权衰减计算总负载，近期负载权重更高
递推公式：$Load(t) = L(t,i) + \gamma \times Load(t)$；32周期负载减半
## 7.5 处理器亲和性
#### 定义
程序手动绑定线程至指定CPU核心，减少跨核迁移、提升缓存命中率
#### 代码示例
```c
#include <sched.h>
#include <sys/sysinfo.h>
#include <stdio.h>
int main() {
	cpu_set_t mask;
	CPU_ZERO(&mask);
	CPU_SET(0, &mask);
	CPU_SET(2, &mask);
	sched_setaffinity(0, sizeof(mask), &mask);
	return 0;
}
```
## 7.6 Linux能耗感知调度
### 7.6.1 功耗模型
功率公式 $W=\frac{1}{2}Vf^2$；固定电压通过调频调节功耗性能
架构：高性能大核、低功耗小核；性能区间0~1024
### 7.6.2 调度策略
- 轻量任务调度至小核，降低功耗
- 密集计算任务分配大核，保障性能
- 平衡整机功耗与运行速度