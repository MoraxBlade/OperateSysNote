# 第9章 同步原语 精简提纲
[TOC]
## 9.1 同步问题的背景
### 9.1.0 什么是同步问题
- 同步问题定义：发送者不可覆盖未读数据，接收者不可读取空/错误数据
- 多核普及加剧同步问题复杂度与重要性
### 9.1.1 多核场景
#### 多核架构兴起
- 单核提升瓶颈：频率升高带来功耗、散热指数增长
- 行业方案：增加CPU核心，桌面/服务器/移动端全多核
#### 多核带来两类核心问题
- 正确性问题：多线程访问共享资源产生竞态，OS提供同步原语解决，但原语引入新开销问题
- 性能可扩展性问题：同步、资源竞争造成性能断崖，软件需适配多核硬件特性
#### 竞态条件（Race Condition）定义
- 多线程操作共享数据，最终结果依赖不确定执行顺序，是同步问题根源
### 9.1.2 生产者消费者模型
#### 模型组成
- 有限FIFO环形缓冲区
- 信号量：empty（空槽，初值容量N）、full（满槽，初值0）
- 生产者：缓冲区未满写入数据
- 消费者：缓冲区有数据取出消费
#### 单生产者单消费者基础实现
- 共享变量、缓冲区读写函数、生产者/消费者代码完整保留
- 单生产者单消费者可正常运行
#### 多生产者存在的竞态问题
- 多生产者同时读写buffer_write_cnt
  - 数据覆盖：多线程写入同一下标
  - 计数错误：两次自增仅生效一次
- 触发条件：读取指针后、写回前线程被打断
### 9.1.3 临界区问题
#### 临界区定义
- 多线程访问共享资源的代码段，同一时间仅允许一个线程进入
#### 生活化类比：卫生间模型
#### 临界区三大硬性要求
- 互斥访问：同一时刻最多一线程进入临界区（核心）
- 空闲让进：无线程占用时，请求线程可立即进入
- 有限等待：线程不会永久等待，无饥饿
#### 单核简易方案：关闭硬件中断
- 原理：单核调度依赖时钟中断，关中断阻止线程切换，保证代码原子执行
- 代码框架保留
- 局限性
  - 多核无效，其他核心仍可并行进入临界区
  - 长时间关中断丢失硬件事件，系统不稳定
## 9.2 互斥锁
### 9.2.0 基本概念
- 互斥锁：最基础同步原语，核心目标消除竞态，保证临界区互斥
### 9.2.1 皮特森算法
- 纯软件两线程互斥方案，无需硬件原子指令
- 核心共享变量：flag[2]、turn
- 完整线程代码保留
- 正确性保证
  - 互斥：同时申请时turn仅单一值，一线程阻塞
  - 读写顺序固定：先置flag，再修改turn，避免交叉判定
- 优点：满足空闲让进、有限等待
- 局限性
  - 原生仅支持双线程
  - CPU乱序执行会失效
  - 忙等持续占用CPU
### 9.2.2 原子操作
#### 原子操作定义
- 操作不可分割，全执行或完全不执行，无中间状态对外可见
#### 常用硬件原子指令
1. CAS（比较交换）
    逻辑等价C代码
    ```c
    int CAS(int *addr, int expected, int new_value) {
       int tmp = *addr;
       if (*addr == expected) {
           *addr = new_value;
       }
       return tmp;
    }
    ```
1. FAA（拿取并累加）
    - 逻辑等价c
        ```c
                
        int FAA(int *addr, int add_value) {
           int tmp = *addr;
           *addr = tmp + add_value;
           return tmp;
        }
        
        ```
    - 适用计数器、排号分配场景
#### 硬件实现机制
- x86：LOCK#锁总线，独占内存访问总线
- ARM：LL/SC加载链接+存储条件，汇编实现CAS完整保留
### 9.2.3 互斥锁标准抽象接口
- lock_init：初始化锁为空闲
- lock：申请互斥锁
- unlock：释放锁，唤醒等待线程
- 加锁安全缓冲区读写代码完整保留
### 9.2.4 自旋锁
#### 概念
- 基于CAS忙等互斥锁：等待线程池逊循环自检
#### 实现
```c
// 自旋锁初始化
void lock_init(int *lock) {
    *lock = 0; // 0表示锁空闲，1表示锁已被占用
}

// 加锁操作
void lock(int *lock) {
    while (atomic_CAS(lock, 0, 1) != 0); // 死循环忙等，直到成功获取锁
}

// 解锁操作
void unlock(int *lock) {
    *lock = 0;
}
```

#### 优缺点
- 优点：实现简单、无上下文切换，短临界区高效
- 缺点：忙等浪费CPU；无公平性，存在饥饿；不满足有限等待
### 9.2.5 排号自旋锁（Ticket Lock）
- 解决普通自旋锁饥饿，实现先到先得公平机制
- 结构体、初始化、加解锁完整代码
- 满足全部临界区三要求：互斥、空闲让进、有限等待
## 9.3 条件变量
### 9.3.1 核心思想
#### 解决忙等
条件不满足时线程阻塞让出CPU，满足后由其他线程唤醒
#### 核心接口分工
- wait阻塞：自己调用
- signal唤醒：其他线程

### 9.3.2 标准接口
- cond_wait(cond, mutex)：原子释放锁+阻塞；唤醒后重获锁
- cond_signal(cond)：唤醒单个等待线程，无等待线程无效果
- cond_broadcast(cond)：唤醒全部阻塞线程
### 9.3.3 生产者消费者改造（消除忙等）
- 完整代码保留
- 关键规范：wait必须包裹while循环，防止虚假唤醒
- 条件变量必须绑定对应互斥锁
### 9.3.4 底层实现
- cond结构体：等待线程链表
- cond_wait / cond_signal / cond_broadcast 底层伪代码完整保留
- 依赖内核wakeup系统调用切换线程状态
### 9.3.5 局限性
- 条件与变量分离，可读性差
- 易匹配错误，误发signal造成永久阻塞死锁
### 9.3.6 互斥锁vs条件变量对比
- 互斥锁：解决临界区互斥；接口同线程成对；仅自旋锁忙等；可独立使用
- 条件变量：解决忙等；wait/signal跨线程；切换阻塞态；必须搭配互斥锁
## 9.4 信号量
### 9.4.1 设计动机与概念
- 解决条件变量条件分离缺陷，资源计数+同步机制一体化
- 信号量：非负整数，正数=可用资源；负数=等待线程数量
- 适用：多线程竞争有限资源；互斥锁侧重单临界区互斥
### 9.4.2 PV原子原语
- P(wait)：资源申请，值-1；<0则线程阻塞入等待队列
- V(signal)：资源释放，值+1；≤0唤醒一个等待线程
### 9.4.3 信号量实现生产者消费者
- 极简代码完整保留，无需额外互斥锁与循环判断
### 9.4.4 底层实现（锁+条件变量封装）
- sem结构体：value、wakeup、内部锁、条件变量
- P操作、V操作底层伪代码完整保留
- wakeup变量：匹配释放资源与唤醒线程数量
### 9.4.5 信号量分类
- 二元信号量：初值1，等价互斥锁
- 计数信号量：初值N(N>1)，资源池、缓冲区限流
### 9.4.6 条件变量vs信号量对比
- 条件变量：条件分离，代码复杂，灵活，易出错，底层锁+cond
- 信号量：条件内置，代码简洁，适配资源计数，低错误率，底层锁+cond
## 9.5 读写锁
### 9.5.1 公告栏场景引入
- 读者：可并行读取；写者：互斥修改；读写互斥
- 普通互斥锁读写全互斥，读多场景性能极差
### 9.5.2 核心规则
- 读者-读者：并行无阻塞
- 读者-写者：互斥
- 写者-写者：互斥
### 9.5.3 两类读写锁（偏向性）
- 偏向读者：新读者可直接进入，读性能高；写者易饥饿
- 偏向写者：写等待时拦截新读者，写无饥饿；读并行度下降
### 9.5.4 偏向读者读写锁实现
```c
struct rwlock {
    int reader_cnt;          // 当前在临界区的读者数量
    struct lock reader_lock; // 保护reader_cnt的互斥锁
    struct lock writer_lock; // 实现读者与写者、写者与写者互斥的锁
};
```
### 9.5.5 偏向写者读写锁实现
增加 'has_writer' 标志位
```c
struct rwlock {
    volatile int reader_cnt;     // 当前在临界区的读者数量
    volatile bool has_writer;    // 是否有写者正在等待或执行
    struct lock lock;            // 保护内部状态的互斥锁
    struct cond reader_cond;     // 读者等待的条件变量
    struct cond writer_cond;     // 写者等待的条件变量
};
```
### 9.5.6 读写锁对比与适用场景
- 偏向读者：读多写少、允许写延迟（配置缓存）
- 偏向写者：读写均衡、写实时要求高（数据库）
## 9.6 RCU锁（读复制更新）
### 9.6.1 设计背景与核心概念
- 读写锁读端存在原子/缓存开销；RCU面向极端读多写少场景
- 核心：读者无锁，同步开销全部转移至写端
- 三阶段：Read无锁读、Copy副本修改、Update原子替换指针+回收旧数据
### 9.6.2 RCU完整流程
1. 读：直接访问当前版本，无锁
2. 复制：写者新建副本修改，不影响在线读者
3. 原子发布：指针原子指向新版本
4. 宽限期：等待所有旧读者退出临界区
5. 释放旧内存
### 9.6.3 宽限期（Grace Period）
- 定义：发布新指针后，等待所有旧访问线程退出的时间段
- 必要性：防止读者访问已释放旧内存
- 时序表格完整保留
### 9.6.4 RCU读者规范
- 核心接口：rcu_read_lock / rcu_read_unlock / rcu_dereference / rcu_assign_pointer
- 强约束：读临界区禁止睡眠、拿普通锁、缺页，临界区尽量短
### 9.6.5 RCU写者流程代码
- 完整链表节点替换代码保留
- 自旋锁仅用于多写者互斥，不阻塞读者
### 9.6.6 RCU优缺点
- 优点：读者零开销、无饥饿、超高并行、无读写死锁
- 缺点：写开销大、旧内存延迟释放、使用约束严格、仅适合极多读场景
### 9.6.7 RCU vs 读写锁对比
- 读者开销：读写锁低；RCU极低
- 写开销：读写锁中等；RCU高
- 写饥饿：读写锁（读偏向存在）；RCU无
- 适用：读写锁读占比90%；RCU读占比99%内核路由/缓存
## 9.7 同步原语衍生问题
### 9.7.1 死锁
#### 定义
多线程循环互相持有等待资源，永久阻塞
#### 典型案例
- 哲学家就餐、十字路口、反向顺序加锁代码
#### 死锁四大必要条件（必须同时满足）
  - 互斥
  - 持有并等待
  - 资源不可抢占
  - 循环等待（成环）
#### 三大解决思路
  1. 检测恢复：资源分配图判环，终止/回滚死锁线程
  2. 死锁预防：破坏四大条件其一
      - 破坏互斥：代理线程执行临界区
      - 破坏持有等待：一次性申请全部资源（trylock代码）
      - 破坏不可抢占：资源可抢占回滚
      - 破坏循环等待：全局统一资源申请顺序
  3. 死锁避免：银行家算法运行时预判不安全状态
### 9.7.2 银行家算法
- 类比银行放贷，预判分配后系统是否存在安全执行序列
- 四大核心数据：最大需求、已分配、剩余需求、可用资源
- 安全性检查步骤
  1. 临时可用=系统可用
  2. 寻找剩余需求≤临时可用的线程
  3. 模拟完成，回收资源更新临时可用
  4. 可全部执行=安全；否则不安全
- 示例推演完整保留
### 9.7.3 活锁
- 定义：线程持续运行、反复重试，但无法推进任务
  - 通常由"不允许持有并等待"的死锁预防方法导致：
- 典型trylock正反线程代码保留
### 活锁vs死锁对比
  - 死锁：线程阻塞、无CPU占用，无法自行恢复
  - 活锁：线程持续占用CPU，极小概率自行恢复
### 9.7.4 优先级反转
- 场景：高优先级线程被低优先级线程阻塞，中间优先级线程持续抢占CPU
- 三线程时序流程保留
- 标准解决方案：优先级继承协议
  - 低优先级持有资源时，临时提升至等待线程最高优先级
  - 释放资源恢复原有优先级，阻断中间线程抢占